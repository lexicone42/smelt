// Generated by smelt-codegen batch (aws) — do not edit by hand.
// Service: apigateway
// Resources: 2

use crate::provider::*;
use std::collections::HashMap;

use super::AwsProvider;

impl AwsProvider {
    pub(super) fn apigateway_api_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "apigateway.Api".into(),
            description: "Api resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "API description".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "API name".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "network".into(),
                        description: "Network configuration".into(),
                        fields: vec![crate::provider::FieldSchema {
                            name: "protocol_type".into(),
                            description: "Protocol type (HTTP or WEBSOCKET)".into(),
                            field_type: crate::provider::FieldType::String,
                            required: true,
                            default: None,
                            sensitive: false,
                        }],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_apigateway_api(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let description = config
            .optional_str("/identity/description")
            .map(String::from);
        let name = config.require_str("/identity/name")?.to_string();
        let protocol_type = config.require_str("/network/protocol_type")?.to_string();

        let tags = super::extract_tags(config);

        let mut req = self
            .apigateway_client
            .create_api()
            .name(&name)
            .protocol_type(aws_sdk_apigatewayv2::types::ProtocolType::from(
                protocol_type.as_str(),
            ));

        if let Some(ref v) = description {
            req = req.description(v);
        }
        req = req.set_tags(Some(tags));

        let result = req
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("create_api: {e}")))?;

        let provider_id = result
            .api_id()
            .ok_or_else(|| ProviderError::ApiError("create_api returned no api_id".into()))?;

        self.read_apigateway_api(provider_id).await
    }

    pub(super) async fn read_apigateway_api(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let result = self
            .apigateway_client
            .get_api()
            .api_id(provider_id)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("get_api: {e}")))?;

        let resource = &result;

        let state = serde_json::json!({
            "identity": {
                "description": resource.description().unwrap_or(""),
                "name": resource.name().unwrap_or(""),
            },
            "network": {
                "protocol_type": resource.protocol_type().map(|r| r.as_str()).unwrap_or(""),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("api_id".into(), serde_json::json!(provider_id));
        outputs.insert(
            "api_endpoint".into(),
            serde_json::json!(resource.api_endpoint().unwrap_or("")),
        );

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_apigateway_api(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let description = config.optional_str("/identity/description");

        let mut req = self.apigateway_client.update_api().api_id(provider_id);

        if let Some(v) = description {
            req = req.description(v);
        }

        req.send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("update_api: {e}")))?;

        self.read_apigateway_api(provider_id).await
    }

    pub(super) async fn delete_apigateway_api(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        self.apigateway_client
            .delete_api()
            .api_id(provider_id)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("delete_api: {e}")))?;
        Ok(())
    }

    pub(super) fn apigateway_stage_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "apigateway.Stage".into(),
            description: "Stage resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "Stage description".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Stage name".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "network".into(),
                        description: "Network configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "api_id".into(),
                                description: "Parent API ID".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "auto_deploy".into(),
                                description: "Enable automatic deployment".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: Some(serde_json::json!(true)),
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_apigateway_stage(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let api_id = config
            .pointer("/network/api_id")
            .or_else(|| config.get("api_id"))
            .and_then(|v| v.as_str())
            .ok_or_else(|| ProviderError::InvalidConfig("network.api_id is required".into()))?;

        let stage_name = config
            .pointer("/identity/name")
            .and_then(|v| v.as_str())
            .ok_or_else(|| ProviderError::InvalidConfig("identity.name is required".into()))?;

        let description = config
            .pointer("/identity/description")
            .and_then(|v| v.as_str())
            .map(|s| s.to_string());

        let auto_deploy = config
            .pointer("/network/auto_deploy")
            .and_then(|v| v.as_bool())
            .unwrap_or(true);

        self.apigateway_client
            .create_stage()
            .api_id(api_id)
            .stage_name(stage_name)
            .set_description(description)
            .auto_deploy(auto_deploy)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("CreateStage: {e}")))?;

        let provider_id_str = format!("{api_id}:{stage_name}");
        let provider_id = provider_id_str.as_str();

        self.read_apigateway_stage(provider_id).await
    }

    pub(super) async fn read_apigateway_stage(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let (api_id, stage_name) = provider_id.split_once(':').ok_or_else(|| {
            ProviderError::InvalidConfig("Stage provider_id must be api_id:stage_name".into())
        })?;

        let result = self
            .apigateway_client
            .get_stage()
            .api_id(api_id)
            .stage_name(stage_name)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("GetStage: {e}")))?;

        let state = serde_json::json!({
            "identity": {
                "name": result.stage_name().unwrap_or(""),
                "description": result.description().unwrap_or(""),
            },
            "network": {
                "auto_deploy": result.auto_deploy(),
                "api_id": api_id,
            }
        });

        let mut outputs = HashMap::new();
        outputs.insert(
            "stage_name".into(),
            serde_json::json!(result.stage_name().unwrap_or("")),
        );
        outputs.insert(
            "invoke_url".into(),
            serde_json::json!(format!(
                "https://{api_id}.execute-api.amazonaws.com/{}",
                result.stage_name().unwrap_or("")
            )),
        );

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_apigateway_stage(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let (api_id, stage_name) = provider_id.split_once(':').ok_or_else(|| {
            ProviderError::InvalidConfig("Stage provider_id must be api_id:stage_name".into())
        })?;

        let description = config
            .pointer("/identity/description")
            .and_then(|v| v.as_str())
            .map(|s| s.to_string());

        let auto_deploy = config
            .pointer("/network/auto_deploy")
            .and_then(|v| v.as_bool())
            .unwrap_or(true);

        self.apigateway_client
            .update_stage()
            .api_id(api_id)
            .stage_name(stage_name)
            .set_description(description)
            .auto_deploy(auto_deploy)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("UpdateStage: {e}")))?;

        self.read_apigateway_stage(provider_id).await
    }

    pub(super) async fn delete_apigateway_stage(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let (api_id, stage_name) = provider_id.split_once(':').ok_or_else(|| {
            ProviderError::InvalidConfig("Stage provider_id must be api_id:stage_name".into())
        })?;

        self.apigateway_client
            .delete_stage()
            .api_id(api_id)
            .stage_name(stage_name)
            .send()
            .await
            .map_err(|e| ProviderError::ApiError(format!("DeleteStage: {e}")))?;
        Ok(())
    }
}
