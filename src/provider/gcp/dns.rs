// Generated by smelt-codegen batch — do not edit by hand.
// Service: dns
// Resources: 3

use crate::provider::*;
use std::collections::HashMap;

use super::GcpProvider;

impl GcpProvider {
    pub(super) fn dns_managedzone_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "dns.ManagedZone".into(),
            description: "ManagedZone resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "A mutable string of at most 1024 characters associated with this resource for the user's convenience. Has no effect on the managed zone's function.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "User labels.".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "User assigned name for this resource. Must be unique within the project. The name must be 1-63 characters long, must begin with a letter, end with a letter or digit, and only contain lowercase letters, digits or dashes.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "cloud_logging_config".into(),
                                description: "".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ManagedZoneCloudLoggingConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "dnssec_config".into(),
                                description: "DNSSEC configuration.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ManagedZoneDnsSecConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "forwarding_config".into(),
                                description: "The presence for this field indicates that outbound forwarding is enabled for this zone. The value of this field contains the set of destinations to forward to.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ManagedZoneForwardingConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name_server_set".into(),
                                description: "Optionally specifies the NameServerSet for this ManagedZone. A NameServerSet is a set of DNS name servers that all host the same ManagedZones. Most users leave this field unset. If you need to use this field, contact your account team.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "peering_config".into(),
                                description: "The presence of this field indicates that DNS Peering is enabled for this zone. The value of this field contains the network to peer with.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ManagedZonePeeringConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "reverse_lookup_config".into(),
                                description: "The presence of this field indicates that this is a managed reverse lookup zone and Cloud DNS resolves reverse lookup queries using automatically configured records for VPC resources. This only applies to networks listed under private_visibility_config.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ManagedZoneReverseLookupConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "visibility".into(),
                                description: "The zone's visibility: public zones are exposed to the Internet, while private zones are visible only to Virtual Private Cloud resources.".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["public".into(), "private".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "dns".into(),
                        description: "Dns configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "dns_name".into(),
                                description: "The DNS name of this managed zone, for instance \"example.com.\".".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_dns_managedzone(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        // TODO: extract cloud_logging_config (Nested(ManagedZoneCloudLoggingConfig)) from config["/config/cloud_logging_config"]
        let description = config.optional_str("/identity/description").map(String::from);
        let dns_name = config.optional_str("/dns/dns_name").map(String::from);
        // TODO: extract dnssec_config (Nested(ManagedZoneDnsSecConfig)) from config["/config/dnssec_config"]
        // TODO: extract forwarding_config (Nested(ManagedZoneForwardingConfig)) from config["/config/forwarding_config"]
        // TODO: extract labels (Record) from config["/identity/labels"]
        let name = config.require_str("/identity/name")?.to_string();
        let name_server_set = config.optional_str("/config/name_server_set").map(String::from);
        // TODO: extract peering_config (Nested(ManagedZonePeeringConfig)) from config["/config/peering_config"]
        // TODO: extract reverse_lookup_config (Nested(ManagedZoneReverseLookupConfig)) from config["/config/reverse_lookup_config"]
        // TODO: extract visibility (Enum(Visibility)) from config["/config/visibility"]

        let labels = super::extract_labels(config);
        // Build SDK model
        let mut model = google_cloud_dns_v1::model::ManagedZone::default();
        // TODO: set cloud_logging_config on model via .set_cloud_logging_config()
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = dns_name {
            model = model.set_dns_name(v);
        }
        // TODO: set dnssec_config on model via .set_dnssec_config()
        // TODO: set forwarding_config on model via .set_forwarding_config()
        model = model.set_name(name.clone());
        if let Some(v) = name_server_set {
            model = model.set_name_server_set(v);
        }
        // TODO: set peering_config on model via .set_peering_config()
        // TODO: set reverse_lookup_config on model via .set_reverse_lookup_config()
        // TODO: set visibility on model via .set_visibility()
        model = model.set_labels(labels);

        // Make API call
        self.managed_zones().await?
            .create()
            .set_project(&self.project_id)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create ManagedZone", e))?;

        let provider_id = name.to_string();
        self.read_dns_managedzone(&provider_id).await
    }

    pub(super) async fn read_dns_managedzone(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        let managed_zone = self
            .managed_zones().await?
            .get()
            .set_project(&self.project_id)
            .set_managed_zone(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetManagedZone", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = managed_zone.labels
            .iter()
            .filter(|(k, _)| k.as_str() != "managed_by")
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "description": managed_zone.description.as_deref().unwrap_or(""),
                "labels": user_labels,
                "name": managed_zone.name.as_deref().unwrap_or(""),
            },
            "config": {
                "cloud_logging_config": serde_json::json!(managed_zone.cloud_logging_config) /* TODO: complex type */,
                "dnssec_config": serde_json::json!(managed_zone.dnssec_config) /* TODO: complex type */,
                "forwarding_config": serde_json::json!(managed_zone.forwarding_config) /* TODO: complex type */,
                "name_server_set": managed_zone.name_server_set.as_deref().unwrap_or(""),
                "peering_config": serde_json::json!(managed_zone.peering_config) /* TODO: complex type */,
                "reverse_lookup_config": serde_json::json!(managed_zone.reverse_lookup_config) /* TODO: complex type */,
                "visibility": serde_json::json!(managed_zone.visibility) /* TODO: complex type */,
            },
            "dns": {
                "dns_name": managed_zone.dns_name.as_deref().unwrap_or(""),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("self_link".into(), serde_json::json!(managed_zone.self_link.as_deref().unwrap_or("")));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_dns_managedzone(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        // Extract fields from config
        // TODO: extract cloud_logging_config (Nested(ManagedZoneCloudLoggingConfig)) from config["/config/cloud_logging_config"]
        let description = config.optional_str("/identity/description").map(String::from);
        let dns_name = config.optional_str("/dns/dns_name").map(String::from);
        // TODO: extract dnssec_config (Nested(ManagedZoneDnsSecConfig)) from config["/config/dnssec_config"]
        // TODO: extract forwarding_config (Nested(ManagedZoneForwardingConfig)) from config["/config/forwarding_config"]
        let name_server_set = config.optional_str("/config/name_server_set").map(String::from);
        // TODO: extract peering_config (Nested(ManagedZonePeeringConfig)) from config["/config/peering_config"]
        // TODO: extract reverse_lookup_config (Nested(ManagedZoneReverseLookupConfig)) from config["/config/reverse_lookup_config"]
        // TODO: extract visibility (Enum(Visibility)) from config["/config/visibility"]
        let labels = super::extract_labels(config);

        let mut model = google_cloud_dns_v1::model::ManagedZone::default();
        // TODO: set cloud_logging_config on model via .set_cloud_logging_config()
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = dns_name {
            model = model.set_dns_name(v);
        }
        // TODO: set dnssec_config on model via .set_dnssec_config()
        // TODO: set forwarding_config on model via .set_forwarding_config()
        if let Some(v) = name_server_set {
            model = model.set_name_server_set(v);
        }
        // TODO: set peering_config on model via .set_peering_config()
        // TODO: set reverse_lookup_config on model via .set_reverse_lookup_config()
        // TODO: set visibility on model via .set_visibility()
        model = model.set_labels(labels);

        self.managed_zones().await?
            .patch()
            .set_project(&self.project_id)
            .set_managed_zone(&name)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("patch ManagedZone", e))?;

        self.read_dns_managedzone(provider_id).await
    }

    pub(super) async fn delete_dns_managedzone(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let name = provider_id.to_string();
        self.managed_zones().await?
            .delete()
            .set_project(&self.project_id)
            .set_managed_zone(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteManagedZone", e))?;
        Ok(())
    }


    pub(super) fn dns_recordset_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "dns.RecordSet".into(),
            description: "ResourceRecordSet resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "For example, www.example.com.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "routing_policy".into(),
                                description: "Configures dynamic query responses based on either the geo location of the querying user or a weighted round robin based routing policy. A valid `ResourceRecordSet` contains only `rrdata` (for static resolution) or a `routing_policy` (for dynamic resolution).".into(),
                                field_type: crate::provider::FieldType::String /* Nested(RRSetRoutingPolicy) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "signature_rrdatas".into(),
                                description: "As defined in RFC 4034 (section 3.2).".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "dns".into(),
                        description: "Dns configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "rrdatas".into(),
                                description: "As defined in RFC 1035 (section 5) and RFC 1034 (section 3.6.1) -- see examples.".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "ttl".into(),
                                description: "Number of seconds that this `ResourceRecordSet` can be cached by resolvers.".into(),
                                field_type: crate::provider::FieldType::Integer,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_dns_recordset(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let name = config.require_str("/identity/name")?.to_string();
        // TODO: extract routing_policy (Nested(RRSetRoutingPolicy)) from config["/config/routing_policy"]
        // TODO: extract rrdatas (Array(String)) from config["/dns/rrdatas"]
        // TODO: extract signature_rrdatas (Array(String)) from config["/config/signature_rrdatas"]
        let ttl = config.optional_i64("/dns/ttl");

        // Build SDK model
        let mut model = google_cloud_dns_v1::model::ResourceRecordSet::default();
        model = model.set_name(name.clone());
        // TODO: set routing_policy on model via .set_routing_policy()
        // TODO: set rrdatas on model via .set_rrdatas()
        // TODO: set signature_rrdatas on model via .set_signature_rrdatas()
        if let Some(v) = ttl {
            model = model.set_ttl(v as _);
        }

        // Make API call
        self.resource_record_sets().await?
            .create()
            .set_project(&self.project_id)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create ResourceRecordSet", e))?;

        let provider_id = name.to_string();
        self.read_dns_recordset(&provider_id).await
    }

    pub(super) async fn read_dns_recordset(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        let resource_record_set = self
            .resource_record_sets().await?
            .get()
            .set_project(&self.project_id)
            .set_resource_record_set(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetResourceRecordSet", e))?;

        let state = serde_json::json!({
            "identity": {
                "name": resource_record_set.name.as_deref().unwrap_or(""),
            },
            "config": {
                "routing_policy": serde_json::json!(resource_record_set.routing_policy) /* TODO: complex type */,
                "signature_rrdatas": serde_json::json!(resource_record_set.signature_rrdatas) /* TODO: complex type */,
            },
            "dns": {
                "rrdatas": serde_json::json!(resource_record_set.rrdatas) /* TODO: complex type */,
                "ttl": resource_record_set.ttl.unwrap_or(0),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("self_link".into(), serde_json::json!(resource_record_set.self_link.as_deref().unwrap_or("")));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_dns_recordset(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        // Extract fields from config
        // TODO: extract routing_policy (Nested(RRSetRoutingPolicy)) from config["/config/routing_policy"]
        // TODO: extract rrdatas (Array(String)) from config["/dns/rrdatas"]
        // TODO: extract signature_rrdatas (Array(String)) from config["/config/signature_rrdatas"]
        let ttl = config.optional_i64("/dns/ttl");

        let mut model = google_cloud_dns_v1::model::ResourceRecordSet::default();
        // TODO: set routing_policy on model via .set_routing_policy()
        // TODO: set rrdatas on model via .set_rrdatas()
        // TODO: set signature_rrdatas on model via .set_signature_rrdatas()
        if let Some(v) = ttl {
            model = model.set_ttl(v as _);
        }

        self.resource_record_sets().await?
            .patch()
            .set_project(&self.project_id)
            .set_resource_record_set(&name)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("patch ResourceRecordSet", e))?;

        self.read_dns_recordset(provider_id).await
    }

    pub(super) async fn delete_dns_recordset(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let name = provider_id.to_string();
        self.resource_record_sets().await?
            .delete()
            .set_project(&self.project_id)
            .set_resource_record_set(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteResourceRecordSet", e))?;
        Ok(())
    }


    pub(super) fn dns_policy_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "dns.Policy".into(),
            description: "Policy resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "A mutable string of at most 1024 characters associated with this resource for the user's convenience. Has no effect on the policy's function.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "User-assigned name for this policy.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "dns_64_config".into(),
                                description: "Configurations related to DNS64 for this policy.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(PolicyDns64Config) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "enable_inbound_forwarding".into(),
                                description: "Allows networks bound to this policy to receive DNS queries sent by VMs or applications over VPN connections. When enabled, a virtual IP address is allocated from each of the subnetworks that are bound to this policy.".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "enable_logging".into(),
                                description: "Controls whether logging is enabled for the networks bound to this policy. Defaults to no logging if not set.".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "networks".into(),
                                description: "List of network names specifying networks to which this policy is applied.".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String /* Nested(PolicyNetwork) */)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_dns_policy(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let description = config.optional_str("/identity/description").map(String::from);
        // TODO: extract dns_64_config (Nested(PolicyDns64Config)) from config["/config/dns_64_config"]
        let enable_inbound_forwarding = config.optional_bool("/config/enable_inbound_forwarding");
        let enable_logging = config.optional_bool("/config/enable_logging");
        let name = config.require_str("/identity/name")?.to_string();
        // TODO: extract networks (Array(Nested(PolicyNetwork))) from config["/config/networks"]

        // Build SDK model
        let mut model = google_cloud_dns_v1::model::Policy::default();
        if let Some(v) = description {
            model = model.set_description(v);
        }
        // TODO: set dns_64_config on model via .set_dns_64_config()
        if let Some(v) = enable_inbound_forwarding {
            model = model.set_enable_inbound_forwarding(v);
        }
        if let Some(v) = enable_logging {
            model = model.set_enable_logging(v);
        }
        model = model.set_name(name.clone());
        // TODO: set networks on model via .set_networks()

        // Make API call
        self.policies().await?
            .create()
            .set_project(&self.project_id)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create Policy", e))?;

        let provider_id = name.to_string();
        self.read_dns_policy(&provider_id).await
    }

    pub(super) async fn read_dns_policy(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        let policy = self
            .policies().await?
            .get()
            .set_project(&self.project_id)
            .set_policy(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetPolicy", e))?;

        let state = serde_json::json!({
            "identity": {
                "description": policy.description.as_deref().unwrap_or(""),
                "name": policy.name.as_deref().unwrap_or(""),
            },
            "config": {
                "dns_64_config": serde_json::json!(policy.dns_64_config) /* TODO: complex type */,
                "enable_inbound_forwarding": policy.enable_inbound_forwarding.unwrap_or(false),
                "enable_logging": policy.enable_logging.unwrap_or(false),
                "networks": serde_json::json!(policy.networks) /* TODO: complex type */,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("self_link".into(), serde_json::json!(policy.self_link.as_deref().unwrap_or("")));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_dns_policy(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let name = provider_id.to_string();
        // Extract fields from config
        let description = config.optional_str("/identity/description").map(String::from);
        // TODO: extract dns_64_config (Nested(PolicyDns64Config)) from config["/config/dns_64_config"]
        let enable_inbound_forwarding = config.optional_bool("/config/enable_inbound_forwarding");
        let enable_logging = config.optional_bool("/config/enable_logging");
        // TODO: extract networks (Array(Nested(PolicyNetwork))) from config["/config/networks"]

        let mut model = google_cloud_dns_v1::model::Policy::default();
        if let Some(v) = description {
            model = model.set_description(v);
        }
        // TODO: set dns_64_config on model via .set_dns_64_config()
        if let Some(v) = enable_inbound_forwarding {
            model = model.set_enable_inbound_forwarding(v);
        }
        if let Some(v) = enable_logging {
            model = model.set_enable_logging(v);
        }
        // TODO: set networks on model via .set_networks()

        self.policies().await?
            .patch()
            .set_project(&self.project_id)
            .set_policy(&name)
            .set_body(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("patch Policy", e))?;

        self.read_dns_policy(provider_id).await
    }

    pub(super) async fn delete_dns_policy(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let name = provider_id.to_string();
        self.policies().await?
            .delete()
            .set_project(&self.project_id)
            .set_policy(&name)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeletePolicy", e))?;
        Ok(())
    }


}

// Diff: fields that force replacement
pub(super) fn dns_managedzone_forces_replacement(path: &str) -> bool {
    matches!(path,
        "identity.name"
    )
}

// Diff: fields that force replacement
pub(super) fn dns_recordset_forces_replacement(path: &str) -> bool {
    matches!(path,
        "identity.name"
    )
}

// Diff: fields that force replacement
pub(super) fn dns_policy_forces_replacement(path: &str) -> bool {
    matches!(path,
        "identity.name"
    )
}
