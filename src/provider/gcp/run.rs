// Generated by smelt-codegen batch — do not edit by hand.
// Service: run
// Resources: 2

use crate::provider::*;
use std::collections::HashMap;

use super::GcpProvider;

impl GcpProvider {
    pub(super) fn run_service_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "run.Service".into(),
            description: "Service resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "User-provided description of the Service. This field currently has a".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "Optional. Unstructured key value map that can be used to organize and".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Identifier. The fully qualified name of this Service. In".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "annotations".into(),
                                description: "Optional. Unstructured key value map that may be set by external tools to".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "binary_authorization".into(),
                                description: "Optional. Settings for the Binary Authorization feature.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(BinaryAuthorization) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "build_config".into(),
                                description: "Optional. Configuration for building a Cloud Run function.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(BuildConfig) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "client".into(),
                                description: "Arbitrary identifier for the API client.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "client_version".into(),
                                description: "Arbitrary version identifier for the API client.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "custom_audiences".into(),
                                description: "One or more custom audiences that you want this service to support. Specify".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "default_uri_disabled".into(),
                                description: "Optional. Disables public resolution of the default URI of this service.".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "etag".into(),
                                description: "Optional. A system-generated fingerprint for this version of the".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "iap_enabled".into(),
                                description: "Optional. IAP settings on the Service.".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "ingress".into(),
                                description: "Optional. Provides the ingress settings for this Service. On output,".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["INGRESS_TRAFFIC_UNSPECIFIED".into(), "INGRESS_TRAFFIC_ALL".into(), "INGRESS_TRAFFIC_INTERNAL_ONLY".into(), "INGRESS_TRAFFIC_INTERNAL_LOAD_BALANCER".into(), "INGRESS_TRAFFIC_NONE".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "invoker_iam_disabled".into(),
                                description: "Optional. Disables IAM permission check for run.routes.invoke for callers".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "launch_stage".into(),
                                description: "Optional. The launch stage as defined by [Google Cloud Platform".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["TODO: list variants for LaunchStage".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "multi_region_settings".into(),
                                description: "Optional. Settings for multi-region deployment.".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["TODO: list variants for MultiRegionSettings".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "scaling".into(),
                                description: "Optional. Specifies service-level scaling settings".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ServiceScaling) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "template".into(),
                                description: "Required. The template used to create revisions for this Service.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(RevisionTemplate) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "traffic".into(),
                                description: "Optional. Specifies how to distribute traffic over a collection of".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String /* Nested(TrafficTarget) */)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_run_service(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        // TODO: extract annotations (Record) from config["/config/annotations"]
        // TODO: extract binary_authorization (Nested(BinaryAuthorization)) from config["/config/binary_authorization"]
        // TODO: extract build_config (Nested(BuildConfig)) from config["/config/build_config"]
        let client = config.optional_str("/config/client").map(String::from);
        let client_version = config
            .optional_str("/config/client_version")
            .map(String::from);
        // TODO: extract custom_audiences (Array(String)) from config["/config/custom_audiences"]
        let default_uri_disabled = config.optional_bool("/config/default_uri_disabled");
        let description = config
            .optional_str("/identity/description")
            .map(String::from);
        let etag = config.optional_str("/config/etag").map(String::from);
        let iap_enabled = config.optional_bool("/config/iap_enabled");
        // TODO: extract ingress (Enum(IngressTraffic)) from config["/config/ingress"]
        let invoker_iam_disabled = config.optional_bool("/config/invoker_iam_disabled");
        // TODO: extract labels (Record) from config["/identity/labels"]
        // TODO: extract launch_stage (Enum(LaunchStage)) from config["/config/launch_stage"]
        // TODO: extract multi_region_settings (Enum(MultiRegionSettings)) from config["/config/multi_region_settings"]
        let name = config.require_str("/identity/name")?.to_string();
        // TODO: extract scaling (Nested(ServiceScaling)) from config["/config/scaling"]
        // TODO: extract template (Nested(RevisionTemplate)) from config["/config/template"]
        // TODO: extract traffic (Array(Nested(TrafficTarget))) from config["/config/traffic"]

        let labels = super::extract_labels(config);
        // Build SDK model
        let mut model = google_cloud_run_v2::model::Service::default();
        // TODO: set annotations on model via .set_annotations()
        // TODO: set binary_authorization on model via .set_binary_authorization()
        // TODO: set build_config on model via .set_build_config()
        if let Some(v) = client {
            model = model.set_client(v);
        }
        if let Some(v) = client_version {
            model = model.set_client_version(v);
        }
        // TODO: set custom_audiences on model via .set_custom_audiences()
        if let Some(v) = default_uri_disabled {
            model = model.set_default_uri_disabled(v);
        }
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = etag {
            model = model.set_etag(v);
        }
        if let Some(v) = iap_enabled {
            model = model.set_iap_enabled(v);
        }
        // TODO: set ingress on model via .set_ingress()
        if let Some(v) = invoker_iam_disabled {
            model = model.set_invoker_iam_disabled(v);
        }
        // TODO: set launch_stage on model via .set_launch_stage()
        // TODO: set multi_region_settings on model via .set_multi_region_settings()
        model = model.set_name(name.clone());
        // TODO: set scaling on model via .set_scaling()
        // TODO: set template on model via .set_template()
        // TODO: set traffic on model via .set_traffic()
        model = model.set_labels(labels);

        // Make API call
        let parent = format!("projects/{}/locations/{}", self.project_id, self.region);
        self.run_services()
            .await?
            .create_service()
            .set_parent(&parent)
            .set_service_id(&name)
            .set_service(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_service Service", e))?;

        let provider_id = format!(
            "projects/{}/locations/{}/services/{}",
            self.project_id, self.region, name
        );
        self.read_run_service(&provider_id).await
    }

    pub(super) async fn read_run_service(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let service = self
            .run_services()
            .await?
            .get_service()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetService", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = service
            .labels
            .iter()
            .filter(|(k, _)| k.as_str() != "managed_by")
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "description": service.description.as_str(),
                "labels": user_labels,
                "name": service.name.as_str(),
            },
            "config": {
                "annotations": serde_json::Value::Null /* TODO: annotations is complex type */,
                "binary_authorization": serde_json::Value::Null /* TODO: binary_authorization is complex type */,
                "build_config": serde_json::Value::Null /* TODO: build_config is complex type */,
                "client": service.client.as_str(),
                "client_version": service.client_version.as_str(),
                "custom_audiences": serde_json::Value::Null /* TODO: custom_audiences is complex type */,
                "default_uri_disabled": service.default_uri_disabled,
                "etag": service.etag.as_str(),
                "iap_enabled": service.iap_enabled,
                "ingress": serde_json::Value::Null /* TODO: ingress is complex type */,
                "invoker_iam_disabled": service.invoker_iam_disabled,
                "launch_stage": serde_json::Value::Null /* TODO: launch_stage is complex type */,
                "multi_region_settings": serde_json::Value::Null /* TODO: multi_region_settings is complex type */,
                "scaling": serde_json::Value::Null /* TODO: scaling is complex type */,
                "template": serde_json::Value::Null /* TODO: template is complex type */,
                "traffic": serde_json::Value::Null /* TODO: traffic is complex type */,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&service.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_run_service(
        &self,
        _provider_id: &str,
        _config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        Err(ProviderError::RequiresReplacement(
            "resource does not support in-place update".into(),
        ))
    }

    pub(super) async fn delete_run_service(&self, provider_id: &str) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.run_services()
            .await?
            .delete_service()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteService", e))?;
        Ok(())
    }

    pub(super) fn run_job_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "run.Job".into(),
            description: "Job resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "Unstructured key value map that can be used to organize and categorize".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "The fully qualified name of this Job.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "annotations".into(),
                                description: "Unstructured key value map that may".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "binary_authorization".into(),
                                description: "Settings for the Binary Authorization feature.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(BinaryAuthorization) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "client".into(),
                                description: "Arbitrary identifier for the API client.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "client_version".into(),
                                description: "Arbitrary version identifier for the API client.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "create_execution".into(),
                                description: "".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["TODO: list variants for CreateExecution".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "etag".into(),
                                description: "Optional. A system-generated fingerprint for this version of the".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "launch_stage".into(),
                                description: "The launch stage as defined by [Google Cloud Platform".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["TODO: list variants for LaunchStage".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "template".into(),
                                description: "Required. The template used to create executions for this Job.".into(),
                                field_type: crate::provider::FieldType::String /* Nested(ExecutionTemplate) */,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_run_job(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        // TODO: extract annotations (Record) from config["/config/annotations"]
        // TODO: extract binary_authorization (Nested(BinaryAuthorization)) from config["/config/binary_authorization"]
        let client = config.optional_str("/config/client").map(String::from);
        let client_version = config
            .optional_str("/config/client_version")
            .map(String::from);
        // TODO: extract create_execution (Enum(CreateExecution)) from config["/config/create_execution"]
        let etag = config.optional_str("/config/etag").map(String::from);
        // TODO: extract labels (Record) from config["/identity/labels"]
        // TODO: extract launch_stage (Enum(LaunchStage)) from config["/config/launch_stage"]
        let name = config.require_str("/identity/name")?.to_string();
        // TODO: extract template (Nested(ExecutionTemplate)) from config["/config/template"]

        let labels = super::extract_labels(config);
        // Build SDK model
        let mut model = google_cloud_run_v2::model::Job::default();
        // TODO: set annotations on model via .set_annotations()
        // TODO: set binary_authorization on model via .set_binary_authorization()
        if let Some(v) = client {
            model = model.set_client(v);
        }
        if let Some(v) = client_version {
            model = model.set_client_version(v);
        }
        // TODO: set create_execution on model via .set_create_execution()
        if let Some(v) = etag {
            model = model.set_etag(v);
        }
        // TODO: set launch_stage on model via .set_launch_stage()
        model = model.set_name(name.clone());
        // TODO: set template on model via .set_template()
        model = model.set_labels(labels);

        // Make API call
        let parent = format!("projects/{}/locations/{}", self.project_id, self.region);
        self.run_jobs()
            .await?
            .create_job()
            .set_parent(&parent)
            .set_job_id(&name)
            .set_job(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_job Job", e))?;

        let provider_id = format!(
            "projects/{}/locations/{}/jobs/{}",
            self.project_id, self.region, name
        );
        self.read_run_job(&provider_id).await
    }

    pub(super) async fn read_run_job(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let job = self
            .run_jobs()
            .await?
            .get_job()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetJob", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = job
            .labels
            .iter()
            .filter(|(k, _)| k.as_str() != "managed_by")
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "labels": user_labels,
                "name": job.name.as_str(),
            },
            "config": {
                "annotations": serde_json::Value::Null /* TODO: annotations is complex type */,
                "binary_authorization": serde_json::Value::Null /* TODO: binary_authorization is complex type */,
                "client": job.client.as_str(),
                "client_version": job.client_version.as_str(),
                "create_execution": serde_json::Value::Null /* TODO: create_execution is complex type */,
                "etag": job.etag.as_str(),
                "launch_stage": serde_json::Value::Null /* TODO: launch_stage is complex type */,
                "template": serde_json::Value::Null /* TODO: template is complex type */,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&job.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_run_job(
        &self,
        _provider_id: &str,
        _config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        Err(ProviderError::RequiresReplacement(
            "resource does not support in-place update".into(),
        ))
    }

    pub(super) async fn delete_run_job(&self, provider_id: &str) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.run_jobs()
            .await?
            .delete_job()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteJob", e))?;
        Ok(())
    }
}

// Diff: fields that force replacement
pub(super) fn run_service_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn run_job_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}
