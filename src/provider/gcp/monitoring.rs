// Generated by smelt-codegen batch — do not edit by hand.
// Service: monitoring
// Resources: 4

use crate::provider::*;
use std::collections::HashMap;

use super::GcpProvider;

impl GcpProvider {
    pub(super) fn monitoring_alertpolicy_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "monitoring.AlertPolicy".into(),
            description: "AlertPolicy resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "A short name or phrase used to identify the policy in dashboards,".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Identifier. Required if the policy exists. The resource name for this".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "alert_strategy".into(),
                                description: "Control over how this alerting policy's notification channels are notified.".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "combiner".into(),
                                description: "How to combine the results of multiple conditions to determine if an".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["COMBINE_UNSPECIFIED".into(), "AND".into(), "OR".into(), "AND_WITH_MATCHING_RESOURCE".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "conditions".into(),
                                description: "A list of conditions for the policy. The conditions are combined by AND or".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "creation_record".into(),
                                description: "A read-only record of the creation of the alerting policy. If provided".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "documentation".into(),
                                description: "Documentation that is included with notifications and incidents related to".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "enabled".into(),
                                description: "Whether or not the policy is enabled. On write, the default interpretation".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "mutation_record".into(),
                                description: "A read-only record of the most recent change to the alerting policy. If".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "notification_channels".into(),
                                description: "Identifies the notification channels to which notifications should be sent".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "severity".into(),
                                description: "Optional. The severity of an alerting policy indicates how important".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["SEVERITY_UNSPECIFIED".into(), "CRITICAL".into(), "ERROR".into(), "WARNING".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "user_labels".into(),
                                description: "User-supplied key/value data to be used for organizing and".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "validity".into(),
                                description: "Read-only description of how the alerting policy is invalid. This field is".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_monitoring_alertpolicy(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let alert_strategy = config.pointer("/config/alert_strategy").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_monitoring_v3::model::alert_policy::AlertStrategy,
                >(v.clone())
                .ok()
        });
        let combiner = config.optional_str("/config/combiner").map(String::from);
        let conditions = config.pointer("/config/conditions").and_then(|v| {
            serde_json::from_value::<
                    Vec<google_cloud_monitoring_v3::model::alert_policy::Condition>,
                >(v.clone())
                .ok()
        });
        let creation_record = config.pointer("/config/creation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let documentation = config.pointer("/config/documentation").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_monitoring_v3::model::alert_policy::Documentation,
                >(v.clone())
                .ok()
        });
        let enabled = config.optional_bool("/config/enabled");
        let mutation_record = config.pointer("/config/mutation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let name = config.require_str("/identity/name")?.to_string();
        let notification_channels = config
            .pointer("/config/notification_channels")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let severity = config.optional_str("/config/severity").map(String::from);
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let validity = config.pointer("/config/validity").and_then(|v| {
            serde_json::from_value::<google_cloud_rpc::model::Status>(v.clone()).ok()
        });

        // Build SDK model
        let mut model = google_cloud_monitoring_v3::model::AlertPolicy::default();
        if let Some(v) = alert_strategy {
            model = model.set_alert_strategy(v);
        }
        if let Some(ref s) = combiner {
            model = model.set_combiner(
                google_cloud_monitoring_v3::model::alert_policy::ConditionCombinerType::from(
                    s.as_str(),
                ),
            );
        }
        if let Some(v) = conditions {
            model = model.set_conditions(v);
        }
        if let Some(v) = creation_record {
            model = model.set_creation_record(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = documentation {
            model = model.set_documentation(v);
        }
        if let Some(v) = enabled {
            model = model.set_enabled(v);
        }
        if let Some(v) = mutation_record {
            model = model.set_mutation_record(v);
        }
        model = model.set_name(name.clone());
        if let Some(v) = notification_channels {
            model = model.set_notification_channels(v);
        }
        if let Some(ref s) = severity {
            model = model.set_severity(
                google_cloud_monitoring_v3::model::alert_policy::Severity::from(s.as_str()),
            );
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }
        if let Some(v) = validity {
            model = model.set_validity(v);
        }

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        self.monitoring()
            .await?
            .create_alert_policy()
            .set_name(&parent)
            .set_alert_policy(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_alert_policy AlertPolicy", e))?;

        let provider_id = format!("projects/{}/alertPolicies/{}", self.project_id, name);
        self.read_monitoring_alertpolicy(&provider_id).await
    }

    pub(super) async fn read_monitoring_alertpolicy(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let alert_policy = self
            .monitoring()
            .await?
            .get_alert_policy()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetAlertPolicy", e))?;

        let state = serde_json::json!({
            "identity": {
                "display_name": alert_policy.display_name.as_str(),
                "name": alert_policy.name.as_str(),
            },
            "config": {
                "alert_strategy": &alert_policy.alert_strategy,
                "combiner": &alert_policy.combiner,
                "conditions": &alert_policy.conditions,
                "creation_record": &alert_policy.creation_record,
                "documentation": &alert_policy.documentation,
                "enabled": alert_policy.enabled.unwrap_or(false),
                "mutation_record": &alert_policy.mutation_record,
                "notification_channels": &alert_policy.notification_channels,
                "severity": &alert_policy.severity,
                "user_labels": &alert_policy.user_labels,
                "validity": &alert_policy.validity,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&alert_policy.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_monitoring_alertpolicy(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let alert_strategy = config.pointer("/config/alert_strategy").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_monitoring_v3::model::alert_policy::AlertStrategy,
                >(v.clone())
                .ok()
        });
        let combiner = config.optional_str("/config/combiner").map(String::from);
        let conditions = config.pointer("/config/conditions").and_then(|v| {
            serde_json::from_value::<
                    Vec<google_cloud_monitoring_v3::model::alert_policy::Condition>,
                >(v.clone())
                .ok()
        });
        let creation_record = config.pointer("/config/creation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let documentation = config.pointer("/config/documentation").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_monitoring_v3::model::alert_policy::Documentation,
                >(v.clone())
                .ok()
        });
        let enabled = config.optional_bool("/config/enabled");
        let mutation_record = config.pointer("/config/mutation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let notification_channels = config
            .pointer("/config/notification_channels")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let severity = config.optional_str("/config/severity").map(String::from);
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let validity = config.pointer("/config/validity").and_then(|v| {
            serde_json::from_value::<google_cloud_rpc::model::Status>(v.clone()).ok()
        });

        let mut model = google_cloud_monitoring_v3::model::AlertPolicy::default();
        model = model.set_name(provider_id);
        if let Some(v) = alert_strategy {
            model = model.set_alert_strategy(v);
        }
        if let Some(ref s) = combiner {
            model = model.set_combiner(
                google_cloud_monitoring_v3::model::alert_policy::ConditionCombinerType::from(
                    s.as_str(),
                ),
            );
        }
        if let Some(v) = conditions {
            model = model.set_conditions(v);
        }
        if let Some(v) = creation_record {
            model = model.set_creation_record(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = documentation {
            model = model.set_documentation(v);
        }
        if let Some(v) = enabled {
            model = model.set_enabled(v);
        }
        if let Some(v) = mutation_record {
            model = model.set_mutation_record(v);
        }
        if let Some(v) = notification_channels {
            model = model.set_notification_channels(v);
        }
        if let Some(ref s) = severity {
            model = model.set_severity(
                google_cloud_monitoring_v3::model::alert_policy::Severity::from(s.as_str()),
            );
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }
        if let Some(v) = validity {
            model = model.set_validity(v);
        }

        self.monitoring()
            .await?
            .update_alert_policy()
            .set_alert_policy(model)
            .set_update_mask(google_cloud_wkt::FieldMask::default())
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("update_alert_policy AlertPolicy", e))?;

        self.read_monitoring_alertpolicy(provider_id).await
    }

    pub(super) async fn delete_monitoring_alertpolicy(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.monitoring()
            .await?
            .delete_alert_policy()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteAlertPolicy", e))?;
        Ok(())
    }

    pub(super) fn monitoring_notificationchannel_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "monitoring.NotificationChannel".into(),
            description: "NotificationChannel resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "description".into(),
                                description: "An optional human-readable description of this notification channel. This".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "An optional human-readable name for this notification channel. It is".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "Configuration fields that define the channel and its behavior. The".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Identifier. The full REST resource name for this channel. The format is:".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "type".into(),
                                description: "The type of the notification channel. This field matches the".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "creation_record".into(),
                                description: "Record of the creation of this channel.".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "enabled".into(),
                                description: "Whether notifications are forwarded to the described channel. This makes".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "mutation_records".into(),
                                description: "Records of the modification of this channel.".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "user_labels".into(),
                                description: "User-supplied key/value data that does not need to conform to".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "output".into(),
                        description: "Output configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "verification_status".into(),
                                description: "Indicates whether this channel has been verified or not. On a".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["VERIFICATION_STATUS_UNSPECIFIED".into(), "UNVERIFIED".into(), "VERIFIED".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_monitoring_notificationchannel(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let creation_record = config.pointer("/config/creation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let description = config
            .optional_str("/identity/description")
            .map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let enabled = config.optional_bool("/config/enabled");
        let _labels_val = config
            .pointer("/identity/labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let mutation_records = config.pointer("/config/mutation_records").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_monitoring_v3::model::MutationRecord>>(
                v.clone(),
            )
            .ok()
        });
        let name = config.require_str("/identity/name")?.to_string();
        let type_val = config.optional_str("/identity/type").map(String::from);
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let verification_status = config
            .optional_str("/output/verification_status")
            .map(String::from);

        // NotificationChannel.labels are channel-type-specific config fields
        // (e.g. email_address for email channels), NOT user labels.
        // Do NOT inject managed_by — use the user's labels as-is.
        let labels: HashMap<String, String> = config
            .pointer("/identity/labels")
            .and_then(|v| v.as_object())
            .map(|m| {
                m.iter()
                    .filter_map(|(k, v)| v.as_str().map(|s| (k.clone(), s.to_string())))
                    .collect()
            })
            .unwrap_or_default();
        let _ = &name; // server-assigned name — not set on model
        // Build SDK model
        let mut model = google_cloud_monitoring_v3::model::NotificationChannel::default();
        if let Some(v) = creation_record {
            model = model.set_creation_record(v);
        }
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = enabled {
            model = model.set_enabled(v);
        }
        if let Some(v) = mutation_records {
            model = model.set_mutation_records(v);
        }
        // Do NOT set model.name on create — GCP auto-assigns it.
        if let Some(v) = type_val {
            model = model.set_type(v);
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }
        if let Some(ref s) = verification_status {
            model = model.set_verification_status(
                google_cloud_monitoring_v3::model::notification_channel::VerificationStatus::from(
                    s.as_str(),
                ),
            );
        }
        model = model.set_labels(labels);

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        let created = self
            .notification_channels()
            .await?
            .create_notification_channel()
            .set_name(&parent)
            .set_notification_channel(model)
            .send()
            .await
            .map_err(|e| {
                super::classify_gcp_error("Create_notification_channel NotificationChannel", e)
            })?;

        let provider_id = created.name.clone();
        // Retry read-back: some APIs (e.g. IAM) have eventual consistency
        for attempt in 0..3u32 {
            match self.read_monitoring_notificationchannel(&provider_id).await {
                Ok(result) => return Ok(result),
                Err(ProviderError::NotFound(_)) if attempt < 2 => {
                    tokio::time::sleep(std::time::Duration::from_millis(
                        500 * (attempt as u64 + 1),
                    ))
                    .await;
                }
                Err(e) => return Err(e),
            }
        }
        self.read_monitoring_notificationchannel(&provider_id).await
    }

    pub(super) async fn read_monitoring_notificationchannel(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let notification_channel = self
            .notification_channels()
            .await?
            .get_notification_channel()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetNotificationChannel", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = notification_channel
            .labels
            .iter()
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "description": notification_channel.description.as_str(),
                "display_name": notification_channel.display_name.as_str(),
                "labels": user_labels,
                "name": notification_channel.name.as_str(),
                "type": notification_channel.r#type.as_str(),
            },
            "config": {
                "creation_record": &notification_channel.creation_record,
                "enabled": notification_channel.enabled.unwrap_or(false),
                "mutation_records": &notification_channel.mutation_records,
                "user_labels": &notification_channel.user_labels,
            },
            "output": {
                "verification_status": &notification_channel.verification_status,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&notification_channel.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_monitoring_notificationchannel(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let creation_record = config.pointer("/config/creation_record").and_then(|v| {
            serde_json::from_value::<google_cloud_monitoring_v3::model::MutationRecord>(v.clone())
                .ok()
        });
        let description = config
            .optional_str("/identity/description")
            .map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let enabled = config.optional_bool("/config/enabled");
        let mutation_records = config.pointer("/config/mutation_records").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_monitoring_v3::model::MutationRecord>>(
                v.clone(),
            )
            .ok()
        });
        let type_val = config.optional_str("/identity/type").map(String::from);
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let verification_status = config
            .optional_str("/output/verification_status")
            .map(String::from);
        let labels: HashMap<String, String> = config
            .pointer("/identity/labels")
            .and_then(|v| v.as_object())
            .map(|m| {
                m.iter()
                    .filter_map(|(k, v)| v.as_str().map(|s| (k.clone(), s.to_string())))
                    .collect()
            })
            .unwrap_or_default();

        let mut model = google_cloud_monitoring_v3::model::NotificationChannel::default();
        model = model.set_name(provider_id);
        if let Some(v) = creation_record {
            model = model.set_creation_record(v);
        }
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = enabled {
            model = model.set_enabled(v);
        }
        if let Some(v) = mutation_records {
            model = model.set_mutation_records(v);
        }
        if let Some(v) = type_val {
            model = model.set_type(v);
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }
        if let Some(ref s) = verification_status {
            model = model.set_verification_status(
                google_cloud_monitoring_v3::model::notification_channel::VerificationStatus::from(
                    s.as_str(),
                ),
            );
        }
        model = model.set_labels(labels);

        self.notification_channels()
            .await?
            .update_notification_channel()
            .set_notification_channel(model)
            .set_update_mask(google_cloud_wkt::FieldMask::default())
            .send()
            .await
            .map_err(|e| {
                super::classify_gcp_error("update_notification_channel NotificationChannel", e)
            })?;

        self.read_monitoring_notificationchannel(provider_id).await
    }

    pub(super) async fn delete_monitoring_notificationchannel(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.notification_channels()
            .await?
            .delete_notification_channel()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteNotificationChannel", e))?;
        Ok(())
    }

    pub(super) fn monitoring_uptimecheckconfig_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "monitoring.UptimeCheckConfig".into(),
            description: "UptimeCheckConfig resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "A human-friendly name for the Uptime check configuration. The display name".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Identifier. A unique resource name for this Uptime check configuration. The".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "checker_type".into(),
                                description: "The type of checkers to use to execute the Uptime check.".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["CHECKER_TYPE_UNSPECIFIED".into(), "STATIC_IP_CHECKERS".into(), "VPC_CHECKERS".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "content_matchers".into(),
                                description: "The content that is expected to appear in the data returned by the target".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "period".into(),
                                description: "How often, in seconds, the Uptime check is performed.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "resource".into(),
                                description: "The resource the check is checking. Required.".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "selected_regions".into(),
                                description: "The list of regions from which the check will be run.".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::Enum(vec!["REGION_UNSPECIFIED".into(), "USA".into(), "EUROPE".into(), "SOUTH_AMERICA".into(), "ASIA_PACIFIC".into(), "USA_OREGON".into(), "USA_IOWA".into(), "USA_VIRGINIA".into()]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "timeout".into(),
                                description: "The maximum amount of time to wait for the request to complete (must be".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "user_labels".into(),
                                description: "User-supplied key/value data to be used for organizing and".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_monitoring_uptimecheckconfig(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let checker_type = config
            .optional_str("/config/checker_type")
            .map(String::from);
        let content_matchers = config.pointer("/config/content_matchers").and_then(|v| {
            serde_json::from_value::<
                Vec<google_cloud_monitoring_v3::model::uptime_check_config::ContentMatcher>,
            >(v.clone())
            .ok()
        });
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let name = config.require_str("/identity/name")?.to_string();
        let period = config
            .pointer("/config/period")
            .and_then(|v| serde_json::from_value::<google_cloud_wkt::Duration>(v.clone()).ok());
        let selected_regions = config.pointer("/config/selected_regions").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_monitoring_v3::model::UptimeCheckRegion>>(
                v.clone(),
            )
            .ok()
        });
        let timeout = config
            .pointer("/config/timeout")
            .and_then(|v| serde_json::from_value::<google_cloud_wkt::Duration>(v.clone()).ok());
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());

        let _ = &name; // server-assigned name — not set on model
        // Build SDK model
        let mut model = google_cloud_monitoring_v3::model::UptimeCheckConfig::default();
        if let Some(ref s) = checker_type {
            model = model.set_checker_type(
                google_cloud_monitoring_v3::model::uptime_check_config::CheckerType::from(
                    s.as_str(),
                ),
            );
        }
        if let Some(v) = content_matchers {
            model = model.set_content_matchers(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        // Do NOT set model.name on create — GCP auto-assigns it.
        if let Some(v) = period {
            model = model.set_period(v);
        }
        if let Some(v) = config.pointer("/config/monitored_resource") {
            let parsed: google_cloud_api::model::MonitoredResource =
                serde_json::from_value(v.clone()).map_err(|e| {
                    ProviderError::InvalidConfig(format!("monitored_resource: {e}"))
                })?;
            model = model.set_monitored_resource(parsed);
        } else if let Some(v) = config.pointer("/config/resource_group") {
            let parsed: google_cloud_monitoring_v3::model::uptime_check_config::ResourceGroup =
                serde_json::from_value(v.clone())
                    .map_err(|e| ProviderError::InvalidConfig(format!("resource_group: {e}")))?;
            model = model.set_resource_group(parsed);
        } else if let Some(v) = config.pointer("/config/synthetic_monitor") {
            let parsed: google_cloud_monitoring_v3::model::SyntheticMonitorTarget =
                serde_json::from_value(v.clone())
                    .map_err(|e| ProviderError::InvalidConfig(format!("synthetic_monitor: {e}")))?;
            model = model.set_synthetic_monitor(parsed);
        }
        if let Some(v) = selected_regions {
            model = model.set_selected_regions(v);
        }
        if let Some(v) = timeout {
            model = model.set_timeout(v);
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        let created = self
            .uptime_checks()
            .await?
            .create_uptime_check_config()
            .set_parent(&parent)
            .set_uptime_check_config(model)
            .send()
            .await
            .map_err(|e| {
                super::classify_gcp_error("Create_uptime_check_config UptimeCheckConfig", e)
            })?;

        let provider_id = created.name.clone();
        // Retry read-back: some APIs (e.g. IAM) have eventual consistency
        for attempt in 0..3u32 {
            match self.read_monitoring_uptimecheckconfig(&provider_id).await {
                Ok(result) => return Ok(result),
                Err(ProviderError::NotFound(_)) if attempt < 2 => {
                    tokio::time::sleep(std::time::Duration::from_millis(
                        500 * (attempt as u64 + 1),
                    ))
                    .await;
                }
                Err(e) => return Err(e),
            }
        }
        self.read_monitoring_uptimecheckconfig(&provider_id).await
    }

    pub(super) async fn read_monitoring_uptimecheckconfig(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let uptime_check_config = self
            .uptime_checks()
            .await?
            .get_uptime_check_config()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetUptimeCheckConfig", e))?;

        let state = serde_json::json!({
            "identity": {
                "display_name": uptime_check_config.display_name.as_str(),
                "name": uptime_check_config.name.as_str(),
            },
            "config": {
                "checker_type": &uptime_check_config.checker_type,
                "content_matchers": &uptime_check_config.content_matchers,
                "period": &uptime_check_config.period,
                "resource": serde_json::Value::Null,
                "selected_regions": &uptime_check_config.selected_regions,
                "timeout": &uptime_check_config.timeout,
                "user_labels": &uptime_check_config.user_labels,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&uptime_check_config.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_monitoring_uptimecheckconfig(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let checker_type = config
            .optional_str("/config/checker_type")
            .map(String::from);
        let content_matchers = config.pointer("/config/content_matchers").and_then(|v| {
            serde_json::from_value::<
                Vec<google_cloud_monitoring_v3::model::uptime_check_config::ContentMatcher>,
            >(v.clone())
            .ok()
        });
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let period = config
            .pointer("/config/period")
            .and_then(|v| serde_json::from_value::<google_cloud_wkt::Duration>(v.clone()).ok());
        let selected_regions = config.pointer("/config/selected_regions").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_monitoring_v3::model::UptimeCheckRegion>>(
                v.clone(),
            )
            .ok()
        });
        let timeout = config
            .pointer("/config/timeout")
            .and_then(|v| serde_json::from_value::<google_cloud_wkt::Duration>(v.clone()).ok());
        let user_labels = config
            .pointer("/config/user_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());

        let mut model = google_cloud_monitoring_v3::model::UptimeCheckConfig::default();
        model = model.set_name(provider_id);
        if let Some(ref s) = checker_type {
            model = model.set_checker_type(
                google_cloud_monitoring_v3::model::uptime_check_config::CheckerType::from(
                    s.as_str(),
                ),
            );
        }
        if let Some(v) = content_matchers {
            model = model.set_content_matchers(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = period {
            model = model.set_period(v);
        }
        if let Some(v) = config.pointer("/config/monitored_resource") {
            let parsed: google_cloud_api::model::MonitoredResource =
                serde_json::from_value(v.clone()).map_err(|e| {
                    ProviderError::InvalidConfig(format!("monitored_resource: {e}"))
                })?;
            model = model.set_monitored_resource(parsed);
        } else if let Some(v) = config.pointer("/config/resource_group") {
            let parsed: google_cloud_monitoring_v3::model::uptime_check_config::ResourceGroup =
                serde_json::from_value(v.clone())
                    .map_err(|e| ProviderError::InvalidConfig(format!("resource_group: {e}")))?;
            model = model.set_resource_group(parsed);
        } else if let Some(v) = config.pointer("/config/synthetic_monitor") {
            let parsed: google_cloud_monitoring_v3::model::SyntheticMonitorTarget =
                serde_json::from_value(v.clone())
                    .map_err(|e| ProviderError::InvalidConfig(format!("synthetic_monitor: {e}")))?;
            model = model.set_synthetic_monitor(parsed);
        }
        if let Some(v) = selected_regions {
            model = model.set_selected_regions(v);
        }
        if let Some(v) = timeout {
            model = model.set_timeout(v);
        }
        if let Some(v) = user_labels {
            model = model.set_user_labels(v);
        }

        self.uptime_checks()
            .await?
            .update_uptime_check_config()
            .set_uptime_check_config(model)
            .set_update_mask(google_cloud_wkt::FieldMask::default())
            .send()
            .await
            .map_err(|e| {
                super::classify_gcp_error("update_uptime_check_config UptimeCheckConfig", e)
            })?;

        self.read_monitoring_uptimecheckconfig(provider_id).await
    }

    pub(super) async fn delete_monitoring_uptimecheckconfig(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.uptime_checks()
            .await?
            .delete_uptime_check_config()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteUptimeCheckConfig", e))?;
        Ok(())
    }

    pub(super) fn monitoring_group_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "monitoring.Group".into(),
            description: "Group resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "A user-assigned name for this group, used only for display purposes.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Output only. The name of this group. The format is:".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "filter".into(),
                                description: "The filter used to determine which monitored resources belong to this".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "is_cluster".into(),
                                description: "If true, the members of this group are considered to be a cluster.".into(),
                                field_type: crate::provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "parent_name".into(),
                                description: "The name of the group's parent, if it has one. The format is:".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_monitoring_group(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let filter = config.optional_str("/config/filter").map(String::from);
        let is_cluster = config.optional_bool("/config/is_cluster");
        let name = config.require_str("/identity/name")?.to_string();
        let parent_name = config.optional_str("/config/parent_name").map(String::from);

        // Build SDK model
        let mut model = google_cloud_monitoring_v3::model::Group::default();
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = filter {
            model = model.set_filter(v);
        }
        if let Some(v) = is_cluster {
            model = model.set_is_cluster(v);
        }
        model = model.set_name(name.clone());
        if let Some(v) = parent_name {
            model = model.set_parent_name(v);
        }

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        self.monitoring_groups()
            .await?
            .create_group()
            .set_name(&parent)
            .set_group(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_group Group", e))?;

        let provider_id = format!("projects/{}/groups/{}", self.project_id, name);
        self.read_monitoring_group(&provider_id).await
    }

    pub(super) async fn read_monitoring_group(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let group = self
            .monitoring_groups()
            .await?
            .get_group()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetGroup", e))?;

        let state = serde_json::json!({
            "identity": {
                "display_name": group.display_name.as_str(),
                "name": group.name.as_str(),
            },
            "config": {
                "filter": group.filter.as_str(),
                "is_cluster": group.is_cluster,
                "parent_name": group.parent_name.as_str(),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&group.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_monitoring_group(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let filter = config.optional_str("/config/filter").map(String::from);
        let is_cluster = config.optional_bool("/config/is_cluster");
        let parent_name = config.optional_str("/config/parent_name").map(String::from);

        let mut model = google_cloud_monitoring_v3::model::Group::default();
        model = model.set_name(provider_id);
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = filter {
            model = model.set_filter(v);
        }
        if let Some(v) = is_cluster {
            model = model.set_is_cluster(v);
        }
        if let Some(v) = parent_name {
            model = model.set_parent_name(v);
        }

        self.monitoring_groups()
            .await?
            .update_group()
            .set_group(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("update_group Group", e))?;

        self.read_monitoring_group(provider_id).await
    }

    pub(super) async fn delete_monitoring_group(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.monitoring_groups()
            .await?
            .delete_group()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteGroup", e))?;
        Ok(())
    }
}

// Diff: fields that force replacement
pub(super) fn monitoring_alertpolicy_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn monitoring_notificationchannel_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn monitoring_uptimecheckconfig_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn monitoring_group_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}
