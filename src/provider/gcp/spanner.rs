// Generated by smelt-codegen batch — do not edit by hand.
// Service: spanner
// Resources: 2

use crate::provider::*;
use std::collections::HashMap;

use super::GcpProvider;
use google_cloud_lro::Poller;

impl GcpProvider {
    pub(super) fn spanner_instance_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "spanner.Instance".into(),
            description: "Instance resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "Required. The descriptive name for this instance as it appears in UIs.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "Cloud Labels are a flexible and lightweight mechanism for organizing cloud".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "Required. A unique identifier for the instance, which cannot be changed".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "autoscaling_config".into(),
                                description: "Optional. The autoscaling configuration. Autoscaling is enabled if this".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "config".into(),
                                description: "Required. The name of the instance's configuration. Values are of the form".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "default_backup_schedule_type".into(),
                                description: "Optional. Controls the default backup schedule behavior for new databases".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["DEFAULT_BACKUP_SCHEDULE_TYPE_UNSPECIFIED".into(), "NONE".into(), "AUTOMATIC".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "edition".into(),
                                description: "Optional. The `Edition` of the current instance.".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["EDITION_UNSPECIFIED".into(), "STANDARD".into(), "ENTERPRISE".into(), "ENTERPRISE_PLUS".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "endpoint_uris".into(),
                                description: "Deprecated. This field is not populated.".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "free_instance_metadata".into(),
                                description: "Free instance metadata. Only populated for free instances.".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "node_count".into(),
                                description: "The number of nodes allocated to this instance. At most, one of either".into(),
                                field_type: crate::provider::FieldType::Integer,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "processing_units".into(),
                                description: "The number of processing units allocated to this instance. At most, one of".into(),
                                field_type: crate::provider::FieldType::Integer,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "sizing".into(),
                        description: "Sizing configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "instance_type".into(),
                                description: "The `InstanceType` of the current instance.".into(),
                                field_type: crate::provider::FieldType::Enum(vec!["INSTANCE_TYPE_UNSPECIFIED".into(), "PROVISIONED".into(), "FREE_INSTANCE".into()]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_spanner_instance(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let autoscaling_config = config.pointer("/config/autoscaling_config").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_spanner_admin_instance_v1::model::AutoscalingConfig,
                >(v.clone())
                .ok()
        });
        let config_val = config.optional_str("/config/config").map(String::from);
        let default_backup_schedule_type = config
            .optional_str("/config/default_backup_schedule_type")
            .map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let edition = config.optional_str("/config/edition").map(String::from);
        let endpoint_uris = config
            .pointer("/config/endpoint_uris")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let free_instance_metadata =
            config
                .pointer("/config/free_instance_metadata")
                .and_then(|v| {
                    serde_json::from_value::<
                        google_cloud_spanner_admin_instance_v1::model::FreeInstanceMetadata,
                    >(v.clone())
                    .ok()
                });
        let instance_type = config
            .optional_str("/sizing/instance_type")
            .map(String::from);
        let _labels_val = config
            .pointer("/identity/labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let name = config.require_str("/identity/name")?.to_string();
        let node_count = config.optional_i64("/config/node_count");
        let processing_units = config.optional_i64("/config/processing_units");

        let labels = super::extract_labels(config);
        // Build SDK model
        let mut model = google_cloud_spanner_admin_instance_v1::model::Instance::default();
        if let Some(v) = autoscaling_config {
            model = model.set_autoscaling_config(v);
        }
        if let Some(v) = config_val {
            model = model.set_config(v);
        }
        if let Some(ref s) = default_backup_schedule_type {
            model = model.set_default_backup_schedule_type(google_cloud_spanner_admin_instance_v1::model::instance::DefaultBackupScheduleType::from(s.as_str()));
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(ref s) = edition {
            model = model.set_edition(
                google_cloud_spanner_admin_instance_v1::model::instance::Edition::from(s.as_str()),
            );
        }
        if let Some(v) = endpoint_uris {
            model = model.set_endpoint_uris(v);
        }
        if let Some(v) = free_instance_metadata {
            model = model.set_free_instance_metadata(v);
        }
        if let Some(ref s) = instance_type {
            model = model.set_instance_type(
                google_cloud_spanner_admin_instance_v1::model::instance::InstanceType::from(
                    s.as_str(),
                ),
            );
        }
        model = model.set_name(name.clone());
        if let Some(v) = node_count {
            model = model.set_node_count(i32::try_from(v).map_err(|_| {
                ProviderError::InvalidConfig(format!("node_count: value {v} out of range for i32"))
            })?);
        }
        if let Some(v) = processing_units {
            model = model.set_processing_units(i32::try_from(v).map_err(|_| {
                ProviderError::InvalidConfig(format!(
                    "processing_units: value {v} out of range for i32"
                ))
            })?);
        }
        model = model.set_labels(labels);

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        self.spanner_instance_admin()
            .await?
            .create_instance()
            .set_parent(&parent)
            .set_instance_id(&name)
            .set_instance(model)
            .poller()
            .until_done()
            .await
            .map_err(|e| super::classify_gcp_error("Create_instance Instance", e))?;

        let provider_id = format!("projects/{}/instances/{}", self.project_id, name);
        self.read_spanner_instance(&provider_id).await
    }

    pub(super) async fn read_spanner_instance(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let instance = self
            .spanner_instance_admin()
            .await?
            .get_instance()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetInstance", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = instance
            .labels
            .iter()
            .filter(|(k, _)| k.as_str() != "managed_by")
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "display_name": instance.display_name.as_str(),
                "labels": user_labels,
                "name": instance.name.as_str(),
            },
            "config": {
                "autoscaling_config": &instance.autoscaling_config,
                "config": instance.config.as_str(),
                "default_backup_schedule_type": &instance.default_backup_schedule_type,
                "edition": &instance.edition,
                "endpoint_uris": &instance.endpoint_uris,
                "free_instance_metadata": &instance.free_instance_metadata,
                "node_count": instance.node_count,
                "processing_units": instance.processing_units,
            },
            "sizing": {
                "instance_type": &instance.instance_type,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&instance.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_spanner_instance(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let autoscaling_config = config.pointer("/config/autoscaling_config").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_spanner_admin_instance_v1::model::AutoscalingConfig,
                >(v.clone())
                .ok()
        });
        let config_val = config.optional_str("/config/config").map(String::from);
        let default_backup_schedule_type = config
            .optional_str("/config/default_backup_schedule_type")
            .map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let edition = config.optional_str("/config/edition").map(String::from);
        let endpoint_uris = config
            .pointer("/config/endpoint_uris")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let free_instance_metadata =
            config
                .pointer("/config/free_instance_metadata")
                .and_then(|v| {
                    serde_json::from_value::<
                        google_cloud_spanner_admin_instance_v1::model::FreeInstanceMetadata,
                    >(v.clone())
                    .ok()
                });
        let instance_type = config
            .optional_str("/sizing/instance_type")
            .map(String::from);
        let node_count = config.optional_i64("/config/node_count");
        let processing_units = config.optional_i64("/config/processing_units");
        let labels = super::extract_labels(config);

        let mut model = google_cloud_spanner_admin_instance_v1::model::Instance::default();
        model = model.set_name(provider_id);
        if let Some(v) = autoscaling_config {
            model = model.set_autoscaling_config(v);
        }
        if let Some(v) = config_val {
            model = model.set_config(v);
        }
        if let Some(ref s) = default_backup_schedule_type {
            model = model.set_default_backup_schedule_type(google_cloud_spanner_admin_instance_v1::model::instance::DefaultBackupScheduleType::from(s.as_str()));
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(ref s) = edition {
            model = model.set_edition(
                google_cloud_spanner_admin_instance_v1::model::instance::Edition::from(s.as_str()),
            );
        }
        if let Some(v) = endpoint_uris {
            model = model.set_endpoint_uris(v);
        }
        if let Some(v) = free_instance_metadata {
            model = model.set_free_instance_metadata(v);
        }
        if let Some(ref s) = instance_type {
            model = model.set_instance_type(
                google_cloud_spanner_admin_instance_v1::model::instance::InstanceType::from(
                    s.as_str(),
                ),
            );
        }
        if let Some(v) = node_count {
            model = model.set_node_count(i32::try_from(v).map_err(|_| {
                ProviderError::InvalidConfig(format!("node_count: value {v} out of range for i32"))
            })?);
        }
        if let Some(v) = processing_units {
            model = model.set_processing_units(i32::try_from(v).map_err(|_| {
                ProviderError::InvalidConfig(format!(
                    "processing_units: value {v} out of range for i32"
                ))
            })?);
        }
        model = model.set_labels(labels);

        self.spanner_instance_admin()
            .await?
            .update_instance()
            .set_instance(model)
            .poller()
            .until_done()
            .await
            .map_err(|e| super::classify_gcp_error("update_instance Instance", e))?;

        self.read_spanner_instance(provider_id).await
    }

    pub(super) async fn delete_spanner_instance(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.spanner_instance_admin()
            .await?
            .delete_instance()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteInstance", e))?;
        Ok(())
    }

    pub(super) fn spanner_instanceconfig_schema() -> crate::provider::ResourceTypeInfo {
        crate::provider::ResourceTypeInfo {
            type_path: "spanner.InstanceConfig".into(),
            description: "InstanceConfig resource".into(),
            schema: crate::provider::ResourceSchema {
                sections: vec![
                    crate::provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "display_name".into(),
                                description: "The name of this instance configuration as it appears in UIs.".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "labels".into(),
                                description: "Cloud Labels are a flexible and lightweight mechanism for organizing cloud".into(),
                                field_type: crate::provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "name".into(),
                                description: "A unique identifier for the instance configuration.  Values".into(),
                                field_type: crate::provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "base_config".into(),
                                description: "Base configuration name, e.g. projects//instanceConfigs/nam3,".into(),
                                field_type: crate::provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            crate::provider::FieldSchema {
                                name: "leader_options".into(),
                                description: "Allowed values of the \"default_leader\" schema option for databases in".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    crate::provider::SectionSchema {
                        name: "reliability".into(),
                        description: "Reliability configuration".into(),
                        fields: vec![
                            crate::provider::FieldSchema {
                                name: "replicas".into(),
                                description: "The geographic placement of nodes in this instance configuration and their".into(),
                                field_type: crate::provider::FieldType::Array(Box::new(crate::provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_spanner_instanceconfig(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let base_config = config.optional_str("/config/base_config").map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let _labels_val = config
            .pointer("/identity/labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let leader_options = config
            .pointer("/config/leader_options")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let name = config.require_str("/identity/name")?.to_string();
        let replicas = config.pointer("/reliability/replicas").and_then(|v| {
            serde_json::from_value::<
                    Vec<google_cloud_spanner_admin_instance_v1::model::ReplicaInfo>,
                >(v.clone())
                .ok()
        });

        let labels = super::extract_labels(config);
        // Build SDK model
        let mut model = google_cloud_spanner_admin_instance_v1::model::InstanceConfig::default();
        if let Some(v) = base_config {
            model = model.set_base_config(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = leader_options {
            model = model.set_leader_options(v);
        }
        model = model.set_name(name.clone());
        if let Some(v) = replicas {
            model = model.set_replicas(v);
        }
        model = model.set_labels(labels);

        // Make API call
        let parent = format!("projects/{}", self.project_id);
        self.spanner_instance_admin()
            .await?
            .create_instance_config()
            .set_parent(&parent)
            .set_instance_config_id(&name)
            .set_instance_config(model)
            .poller()
            .until_done()
            .await
            .map_err(|e| super::classify_gcp_error("Create_instance_config InstanceConfig", e))?;

        let provider_id = format!("projects/{}/instanceConfigs/{}", self.project_id, name);
        self.read_spanner_instanceconfig(&provider_id).await
    }

    pub(super) async fn read_spanner_instanceconfig(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let instance_config = self
            .spanner_instance_admin()
            .await?
            .get_instance_config()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetInstanceConfig", e))?;

        let user_labels: serde_json::Map<String, serde_json::Value> = instance_config
            .labels
            .iter()
            .filter(|(k, _)| k.as_str() != "managed_by")
            .map(|(k, v)| (k.clone(), serde_json::json!(v)))
            .collect();

        let state = serde_json::json!({
            "identity": {
                "display_name": instance_config.display_name.as_str(),
                "labels": user_labels,
                "name": instance_config.name.as_str(),
            },
            "config": {
                "base_config": instance_config.base_config.as_str(),
                "leader_options": &instance_config.leader_options,
            },
            "reliability": {
                "replicas": &instance_config.replicas,
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&instance_config.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_spanner_instanceconfig(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let base_config = config.optional_str("/config/base_config").map(String::from);
        let display_name = config
            .optional_str("/identity/display_name")
            .map(String::from);
        let leader_options = config
            .pointer("/config/leader_options")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let replicas = config.pointer("/reliability/replicas").and_then(|v| {
            serde_json::from_value::<
                    Vec<google_cloud_spanner_admin_instance_v1::model::ReplicaInfo>,
                >(v.clone())
                .ok()
        });
        let labels = super::extract_labels(config);

        let mut model = google_cloud_spanner_admin_instance_v1::model::InstanceConfig::default();
        model = model.set_name(provider_id);
        if let Some(v) = base_config {
            model = model.set_base_config(v);
        }
        if let Some(v) = display_name {
            model = model.set_display_name(v);
        }
        if let Some(v) = leader_options {
            model = model.set_leader_options(v);
        }
        if let Some(v) = replicas {
            model = model.set_replicas(v);
        }
        model = model.set_labels(labels);

        self.spanner_instance_admin()
            .await?
            .update_instance_config()
            .set_instance_config(model)
            .set_update_mask(google_cloud_wkt::FieldMask::default())
            .poller()
            .until_done()
            .await
            .map_err(|e| super::classify_gcp_error("update_instance_config InstanceConfig", e))?;

        self.read_spanner_instanceconfig(provider_id).await
    }

    pub(super) async fn delete_spanner_instanceconfig(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.spanner_instance_admin()
            .await?
            .delete_instance_config()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteInstanceConfig", e))?;
        Ok(())
    }
}

// Diff: fields that force replacement
pub(super) fn spanner_instance_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn spanner_instanceconfig_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}
