// Generated by smelt-codegen batch — do not edit by hand.
// Service: container
// Resources: 2

use smelt_provider::*;
use std::collections::HashMap;

use super::GcpProvider;

impl GcpProvider {
    pub(super) fn container_cluster_schema() -> smelt_provider::ResourceTypeInfo {
        smelt_provider::ResourceTypeInfo {
            type_path: "container.Cluster".into(),
            description: "Cluster resource".into(),
            schema: smelt_provider::ResourceSchema {
                sections: vec![
                    smelt_provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            smelt_provider::FieldSchema {
                                name: "description".into(),
                                description: "An optional description of this cluster.".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "name".into(),
                                description: "The name of this cluster. The name must be unique within this project".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    smelt_provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            smelt_provider::FieldSchema {
                                name: "addons_config".into(),
                                description: "Configurations for the various addons available to run in the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "alpha_cluster_feature_gates".into(),
                                description: "The list of user specified Kubernetes feature gates.".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "authenticator_groups_config".into(),
                                description: "Configuration controlling RBAC group membership information.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "autopilot".into(),
                                description: "Autopilot configuration for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "autoscaling".into(),
                                description: "Cluster-level autoscaling configuration.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "binary_authorization".into(),
                                description: "Configuration for Binary Authorization.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "cluster_ipv4_cidr".into(),
                                description: "The IP address range of the container pods in this cluster, in".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "compliance_posture_config".into(),
                                description: "Enable/Disable Compliance Posture features for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "conditions".into(),
                                description: "Which conditions caused the current cluster state.".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "confidential_nodes".into(),
                                description: "Configuration of Confidential Nodes.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "cost_management_config".into(),
                                description: "Configuration for the fine-grained cost management feature.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "database_encryption".into(),
                                description: "Configuration of etcd encryption.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "default_max_pods_constraint".into(),
                                description: "The default constraint on the maximum number of pods that can be run".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "enable_k8s_beta_apis".into(),
                                description: "Beta APIs Config".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "enable_kubernetes_alpha".into(),
                                description: "Kubernetes alpha features are enabled on this cluster. This includes alpha".into(),
                                field_type: smelt_provider::FieldType::Bool,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "fleet".into(),
                                description: "Fleet information for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "gke_auto_upgrade_config".into(),
                                description: "Configuration for GKE auto upgrades.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "identity_service_config".into(),
                                description: "Configuration for Identity Service component.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "initial_cluster_version".into(),
                                description: "The initial Kubernetes version for this cluster.  Valid versions are those".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "ip_allocation_policy".into(),
                                description: "Configuration for cluster IP allocation.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "legacy_abac".into(),
                                description: "Configuration for the legacy ABAC authorization mode.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "locations".into(),
                                description: "The list of Google Compute Engine".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "logging_config".into(),
                                description: "Logging configuration for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "logging_service".into(),
                                description: "The logging service the cluster should use to write logs.".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "maintenance_policy".into(),
                                description: "Configure the maintenance policy for this cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "managed_opentelemetry_config".into(),
                                description: "Configuration for Managed OpenTelemetry pipeline.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "master_auth".into(),
                                description: "The authentication information for accessing the master endpoint.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "mesh_certificates".into(),
                                description: "Configuration for issuance of mTLS keys and certificates to Kubernetes".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "monitoring_config".into(),
                                description: "Monitoring configuration for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "monitoring_service".into(),
                                description: "The monitoring service the cluster should use to write metrics.".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "network_config".into(),
                                description: "Configuration for cluster networking.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "network_policy".into(),
                                description: "Configuration options for the NetworkPolicy feature.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "node_pool_auto_config".into(),
                                description: "Node pool configs that apply to all auto-provisioned node pools".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "node_pool_defaults".into(),
                                description: "Default NodePool settings for the entire cluster. These settings are".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "node_pools".into(),
                                description: "The node pools associated with this cluster.".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "notification_config".into(),
                                description: "Notification configuration of the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "pod_autoscaling".into(),
                                description: "The config for pod autoscaling.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "private_cluster_config".into(),
                                description: "Configuration for private cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "rbac_binding_config".into(),
                                description: "RBACBindingConfig allows user to restrict ClusterRoleBindings an".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "release_channel".into(),
                                description: "Release channel configuration. If left unspecified on cluster creation and".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "resource_labels".into(),
                                description: "The resource labels for the cluster to use to annotate any related".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "resource_usage_export_config".into(),
                                description: "Configuration for exporting resource usages. Resource usage export is".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "secret_manager_config".into(),
                                description: "Secret CSI driver configuration.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: true,
                            },
                            smelt_provider::FieldSchema {
                                name: "security_posture_config".into(),
                                description: "Enable/Disable Security Posture API features for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "shielded_nodes".into(),
                                description: "Shielded Nodes configuration.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "user_managed_keys_config".into(),
                                description: "The Custom keys configuration for the cluster.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "vertical_pod_autoscaling".into(),
                                description: "Cluster-level Vertical Pod Autoscaling configuration.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "workload_identity_config".into(),
                                description: "Configuration for the use of Kubernetes Service Accounts in IAM policies.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    smelt_provider::SectionSchema {
                        name: "network".into(),
                        description: "Network configuration".into(),
                        fields: vec![
                            smelt_provider::FieldSchema {
                                name: "network".into(),
                                description: "The name of the Google Compute Engine".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "subnetwork".into(),
                                description: "The name of the Google Compute Engine".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_container_cluster(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let addons_config = config.pointer("/config/addons_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::AddonsConfig>(v.clone()).ok()
        });
        let alpha_cluster_feature_gates = config
            .pointer("/config/alpha_cluster_feature_gates")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let authenticator_groups_config = config
            .pointer("/config/authenticator_groups_config")
            .and_then(|v| {
                serde_json::from_value::<
                        google_cloud_container_v1::model::AuthenticatorGroupsConfig,
                    >(v.clone())
                    .ok()
            });
        let autopilot = config.pointer("/config/autopilot").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::Autopilot>(v.clone()).ok()
        });
        let autoscaling = config.pointer("/config/autoscaling").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::ClusterAutoscaling>(
                v.clone(),
            )
            .ok()
        });
        let binary_authorization = config
            .pointer("/config/binary_authorization")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::BinaryAuthorization>(
                    v.clone(),
                )
                .ok()
            });
        let cluster_ipv4_cidr = config
            .optional_str("/config/cluster_ipv4_cidr")
            .map(String::from);
        let compliance_posture_config = config
            .pointer("/config/compliance_posture_config")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::CompliancePostureConfig>(
                    v.clone(),
                )
                .ok()
            });
        let conditions = config.pointer("/config/conditions").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_container_v1::model::StatusCondition>>(
                v.clone(),
            )
            .ok()
        });
        let confidential_nodes = config.pointer("/config/confidential_nodes").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::ConfidentialNodes>(v.clone())
                .ok()
        });
        let cost_management_config = config.pointer("/config/cost_management_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::CostManagementConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let database_encryption = config.pointer("/config/database_encryption").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::DatabaseEncryption>(
                v.clone(),
            )
            .ok()
        });
        let default_max_pods_constraint = config
            .pointer("/config/default_max_pods_constraint")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::MaxPodsConstraint>(
                    v.clone(),
                )
                .ok()
            });
        let description = config
            .optional_str("/identity/description")
            .map(String::from);
        let enable_k8s_beta_apis = config
            .pointer("/config/enable_k8s_beta_apis")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::K8sBetaAPIConfig>(
                    v.clone(),
                )
                .ok()
            });
        let enable_kubernetes_alpha = config.optional_bool("/config/enable_kubernetes_alpha");
        let fleet = config.pointer("/config/fleet").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::Fleet>(v.clone()).ok()
        });
        let gke_auto_upgrade_config = config.pointer("/config/gke_auto_upgrade_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::GkeAutoUpgradeConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let identity_service_config = config.pointer("/config/identity_service_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::IdentityServiceConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let initial_cluster_version = config
            .optional_str("/config/initial_cluster_version")
            .map(String::from);
        let ip_allocation_policy = config
            .pointer("/config/ip_allocation_policy")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::IPAllocationPolicy>(
                    v.clone(),
                )
                .ok()
            });
        let legacy_abac = config.pointer("/config/legacy_abac").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::LegacyAbac>(v.clone()).ok()
        });
        let locations = config
            .pointer("/config/locations")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let logging_config = config.pointer("/config/logging_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::LoggingConfig>(v.clone())
                .ok()
        });
        let logging_service = config
            .optional_str("/config/logging_service")
            .map(String::from);
        let maintenance_policy = config.pointer("/config/maintenance_policy").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MaintenancePolicy>(v.clone())
                .ok()
        });
        let managed_opentelemetry_config = config
            .pointer("/config/managed_opentelemetry_config")
            .and_then(|v| {
                serde_json::from_value::<
                        google_cloud_container_v1::model::ManagedOpenTelemetryConfig,
                    >(v.clone())
                    .ok()
            });
        let master_auth = config.pointer("/config/master_auth").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MasterAuth>(v.clone()).ok()
        });
        let mesh_certificates = config.pointer("/config/mesh_certificates").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MeshCertificates>(v.clone())
                .ok()
        });
        let monitoring_config = config.pointer("/config/monitoring_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MonitoringConfig>(v.clone())
                .ok()
        });
        let monitoring_service = config
            .optional_str("/config/monitoring_service")
            .map(String::from);
        let name = config.require_str("/identity/name")?.to_string();
        let network = config.optional_str("/network/network").map(String::from);
        let network_config = config.pointer("/config/network_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NetworkConfig>(v.clone())
                .ok()
        });
        let network_policy = config.pointer("/config/network_policy").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NetworkPolicy>(v.clone())
                .ok()
        });
        let node_pool_auto_config = config
            .pointer("/config/node_pool_auto_config")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::NodePoolAutoConfig>(
                    v.clone(),
                )
                .ok()
            });
        let node_pool_defaults = config.pointer("/config/node_pool_defaults").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodePoolDefaults>(v.clone())
                .ok()
        });
        let node_pools = config.pointer("/config/node_pools").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_container_v1::model::NodePool>>(v.clone())
                .ok()
        });
        let notification_config = config.pointer("/config/notification_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NotificationConfig>(
                v.clone(),
            )
            .ok()
        });
        let pod_autoscaling = config.pointer("/config/pod_autoscaling").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::PodAutoscaling>(v.clone())
                .ok()
        });
        let private_cluster_config = config.pointer("/config/private_cluster_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::PrivateClusterConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let rbac_binding_config = config.pointer("/config/rbac_binding_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::RBACBindingConfig>(v.clone())
                .ok()
        });
        let release_channel = config.pointer("/config/release_channel").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::ReleaseChannel>(v.clone())
                .ok()
        });
        let resource_labels = config
            .pointer("/config/resource_labels")
            .and_then(|v| serde_json::from_value::<HashMap<String, String>>(v.clone()).ok());
        let resource_usage_export_config = config
            .pointer("/config/resource_usage_export_config")
            .and_then(|v| {
                serde_json::from_value::<
                        google_cloud_container_v1::model::ResourceUsageExportConfig,
                    >(v.clone())
                    .ok()
            });
        let secret_manager_config = config
            .pointer("/config/secret_manager_config")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::SecretManagerConfig>(
                    v.clone(),
                )
                .ok()
            });
        let security_posture_config = config.pointer("/config/security_posture_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::SecurityPostureConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let shielded_nodes = config.pointer("/config/shielded_nodes").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::ShieldedNodes>(v.clone())
                .ok()
        });
        let subnetwork = config.optional_str("/network/subnetwork").map(String::from);
        let user_managed_keys_config = config.pointer("/config/user_managed_keys_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::UserManagedKeysConfig>(
                    v.clone(),
                )
                .ok()
            },
        );
        let vertical_pod_autoscaling = config.pointer("/config/vertical_pod_autoscaling").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::VerticalPodAutoscaling>(
                    v.clone(),
                )
                .ok()
            },
        );
        let workload_identity_config = config.pointer("/config/workload_identity_config").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::WorkloadIdentityConfig>(
                    v.clone(),
                )
                .ok()
            },
        );

        // Build SDK model
        let mut model = google_cloud_container_v1::model::Cluster::default();
        if let Some(v) = addons_config {
            model = model.set_addons_config(v);
        }
        if let Some(v) = alpha_cluster_feature_gates {
            model = model.set_alpha_cluster_feature_gates(v);
        }
        if let Some(v) = authenticator_groups_config {
            model = model.set_authenticator_groups_config(v);
        }
        if let Some(v) = autopilot {
            model = model.set_autopilot(v);
        }
        if let Some(v) = autoscaling {
            model = model.set_autoscaling(v);
        }
        if let Some(v) = binary_authorization {
            model = model.set_binary_authorization(v);
        }
        if let Some(v) = cluster_ipv4_cidr {
            model = model.set_cluster_ipv4_cidr(v);
        }
        if let Some(v) = compliance_posture_config {
            model = model.set_compliance_posture_config(v);
        }
        if let Some(v) = conditions {
            model = model.set_conditions(v);
        }
        if let Some(v) = confidential_nodes {
            model = model.set_confidential_nodes(v);
        }
        if let Some(v) = cost_management_config {
            model = model.set_cost_management_config(v);
        }
        if let Some(v) = database_encryption {
            model = model.set_database_encryption(v);
        }
        if let Some(v) = default_max_pods_constraint {
            model = model.set_default_max_pods_constraint(v);
        }
        if let Some(v) = description {
            model = model.set_description(v);
        }
        if let Some(v) = enable_k8s_beta_apis {
            model = model.set_enable_k8s_beta_apis(v);
        }
        if let Some(v) = enable_kubernetes_alpha {
            model = model.set_enable_kubernetes_alpha(v);
        }
        if let Some(v) = fleet {
            model = model.set_fleet(v);
        }
        if let Some(v) = gke_auto_upgrade_config {
            model = model.set_gke_auto_upgrade_config(v);
        }
        if let Some(v) = identity_service_config {
            model = model.set_identity_service_config(v);
        }
        if let Some(v) = initial_cluster_version {
            model = model.set_initial_cluster_version(v);
        }
        if let Some(v) = ip_allocation_policy {
            model = model.set_ip_allocation_policy(v);
        }
        if let Some(v) = legacy_abac {
            model = model.set_legacy_abac(v);
        }
        if let Some(v) = locations {
            model = model.set_locations(v);
        }
        if let Some(v) = logging_config {
            model = model.set_logging_config(v);
        }
        if let Some(v) = logging_service {
            model = model.set_logging_service(v);
        }
        if let Some(v) = maintenance_policy {
            model = model.set_maintenance_policy(v);
        }
        if let Some(v) = managed_opentelemetry_config {
            model = model.set_managed_opentelemetry_config(v);
        }
        if let Some(v) = master_auth {
            model = model.set_master_auth(v);
        }
        if let Some(v) = mesh_certificates {
            model = model.set_mesh_certificates(v);
        }
        if let Some(v) = monitoring_config {
            model = model.set_monitoring_config(v);
        }
        if let Some(v) = monitoring_service {
            model = model.set_monitoring_service(v);
        }
        model = model.set_name(name.clone());
        if let Some(v) = network {
            model = model.set_network(v);
        }
        if let Some(v) = network_config {
            model = model.set_network_config(v);
        }
        if let Some(v) = network_policy {
            model = model.set_network_policy(v);
        }
        if let Some(v) = node_pool_auto_config {
            model = model.set_node_pool_auto_config(v);
        }
        if let Some(v) = node_pool_defaults {
            model = model.set_node_pool_defaults(v);
        }
        if let Some(v) = node_pools {
            model = model.set_node_pools(v);
        }
        if let Some(v) = notification_config {
            model = model.set_notification_config(v);
        }
        if let Some(v) = pod_autoscaling {
            model = model.set_pod_autoscaling(v);
        }
        if let Some(v) = private_cluster_config {
            model = model.set_private_cluster_config(v);
        }
        if let Some(v) = rbac_binding_config {
            model = model.set_rbac_binding_config(v);
        }
        if let Some(v) = release_channel {
            model = model.set_release_channel(v);
        }
        if let Some(v) = resource_labels {
            model = model.set_resource_labels(v);
        }
        if let Some(v) = resource_usage_export_config {
            model = model.set_resource_usage_export_config(v);
        }
        if let Some(v) = secret_manager_config {
            model = model.set_secret_manager_config(v);
        }
        if let Some(v) = security_posture_config {
            model = model.set_security_posture_config(v);
        }
        if let Some(v) = shielded_nodes {
            model = model.set_shielded_nodes(v);
        }
        if let Some(v) = subnetwork {
            model = model.set_subnetwork(v);
        }
        if let Some(v) = user_managed_keys_config {
            model = model.set_user_managed_keys_config(v);
        }
        if let Some(v) = vertical_pod_autoscaling {
            model = model.set_vertical_pod_autoscaling(v);
        }
        if let Some(v) = workload_identity_config {
            model = model.set_workload_identity_config(v);
        }

        // Make API call
        let parent = format!("projects/{}/locations/{}", self.project_id, self.region);
        self.cluster_manager()
            .await?
            .create_cluster()
            .set_parent(&parent)
            .set_cluster(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_cluster Cluster", e))?;

        let provider_id = format!(
            "projects/{}/locations/{}/clusters/{}",
            self.project_id, self.region, name
        );

        // GKE cluster creation is a long-running operation (~5-10 minutes).
        // Poll until the cluster reaches RUNNING status before read-back.
        for attempt in 0..60u32 {
            match self.read_container_cluster(&provider_id).await {
                Ok(result) => return Ok(result),
                Err(ProviderError::NotFound(_)) if attempt < 59 => {
                    tokio::time::sleep(std::time::Duration::from_secs(10)).await;
                }
                Err(e) => {
                    if attempt < 59 {
                        tokio::time::sleep(std::time::Duration::from_secs(10)).await;
                        continue;
                    }
                    return Err(e);
                }
            }
        }
        self.read_container_cluster(&provider_id).await
    }

    pub(super) async fn read_container_cluster(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let cluster = self
            .cluster_manager()
            .await?
            .get_cluster()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetCluster", e))?;

        let state = serde_json::json!({
            "identity": {
                "description": cluster.description.as_str(),
                "name": cluster.name.as_str(),
            },
            "config": {
                "addons_config": &cluster.addons_config,
                "alpha_cluster_feature_gates": &cluster.alpha_cluster_feature_gates,
                "authenticator_groups_config": &cluster.authenticator_groups_config,
                "autopilot": &cluster.autopilot,
                "autoscaling": &cluster.autoscaling,
                "binary_authorization": &cluster.binary_authorization,
                "cluster_ipv4_cidr": cluster.cluster_ipv4_cidr.as_str(),
                "compliance_posture_config": &cluster.compliance_posture_config,
                "conditions": &cluster.conditions,
                "confidential_nodes": &cluster.confidential_nodes,
                "cost_management_config": &cluster.cost_management_config,
                "database_encryption": &cluster.database_encryption,
                "default_max_pods_constraint": &cluster.default_max_pods_constraint,
                "enable_k8s_beta_apis": &cluster.enable_k8s_beta_apis,
                "enable_kubernetes_alpha": cluster.enable_kubernetes_alpha,
                "fleet": &cluster.fleet,
                "gke_auto_upgrade_config": &cluster.gke_auto_upgrade_config,
                "identity_service_config": &cluster.identity_service_config,
                "initial_cluster_version": cluster.initial_cluster_version.as_str(),
                "ip_allocation_policy": &cluster.ip_allocation_policy,
                "legacy_abac": &cluster.legacy_abac,
                "locations": &cluster.locations,
                "logging_config": &cluster.logging_config,
                "logging_service": cluster.logging_service.as_str(),
                "maintenance_policy": &cluster.maintenance_policy,
                "managed_opentelemetry_config": &cluster.managed_opentelemetry_config,
                "master_auth": &cluster.master_auth,
                "mesh_certificates": &cluster.mesh_certificates,
                "monitoring_config": &cluster.monitoring_config,
                "monitoring_service": cluster.monitoring_service.as_str(),
                "network_config": &cluster.network_config,
                "network_policy": &cluster.network_policy,
                "node_pool_auto_config": &cluster.node_pool_auto_config,
                "node_pool_defaults": &cluster.node_pool_defaults,
                "node_pools": &cluster.node_pools,
                "notification_config": &cluster.notification_config,
                "pod_autoscaling": &cluster.pod_autoscaling,
                "private_cluster_config": &cluster.private_cluster_config,
                "rbac_binding_config": &cluster.rbac_binding_config,
                "release_channel": &cluster.release_channel,
                "resource_labels": &cluster.resource_labels,
                "resource_usage_export_config": &cluster.resource_usage_export_config,
                "secret_manager_config": &cluster.secret_manager_config,
                "security_posture_config": &cluster.security_posture_config,
                "shielded_nodes": &cluster.shielded_nodes,
                "user_managed_keys_config": &cluster.user_managed_keys_config,
                "vertical_pod_autoscaling": &cluster.vertical_pod_autoscaling,
                "workload_identity_config": &cluster.workload_identity_config,
            },
            "network": {
                "network": cluster.network.as_str(),
                "subnetwork": cluster.subnetwork.as_str(),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&cluster.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_container_cluster(
        &self,
        _provider_id: &str,
        _config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        Err(ProviderError::RequiresReplacement(
            "resource does not support in-place update".into(),
        ))
    }

    pub(super) async fn delete_container_cluster(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.cluster_manager()
            .await?
            .delete_cluster()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteCluster", e))?;
        Ok(())
    }

    pub(super) fn container_nodepool_schema() -> smelt_provider::ResourceTypeInfo {
        smelt_provider::ResourceTypeInfo {
            type_path: "container.NodePool".into(),
            description: "NodePool resource".into(),
            schema: smelt_provider::ResourceSchema {
                sections: vec![
                    smelt_provider::SectionSchema {
                        name: "identity".into(),
                        description: "Identity configuration".into(),
                        fields: vec![
                            smelt_provider::FieldSchema {
                                name: "cluster_id".into(),
                                description: "Provider ID of the parent resource (injected via binding)".into(),
                                field_type: smelt_provider::FieldType::Ref("*".into()),
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "name".into(),
                                description: "The name of the node pool.".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: true,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                    smelt_provider::SectionSchema {
                        name: "config".into(),
                        description: "Config configuration".into(),
                        fields: vec![
                            smelt_provider::FieldSchema {
                                name: "autoscaling".into(),
                                description: "Autoscaler configuration for this NodePool. Autoscaler is enabled".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "best_effort_provisioning".into(),
                                description: "Enable best effort provisioning for nodes".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "conditions".into(),
                                description: "Which conditions caused the current node pool state.".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::Record(vec![]))),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "config".into(),
                                description: "The node configuration of the pool.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "initial_node_count".into(),
                                description: "The initial node count for the pool. You must ensure that your".into(),
                                field_type: smelt_provider::FieldType::Integer,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "locations".into(),
                                description: "The list of Google Compute Engine".into(),
                                field_type: smelt_provider::FieldType::Array(Box::new(smelt_provider::FieldType::String)),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "management".into(),
                                description: "NodeManagement configuration for this NodePool.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "max_pods_constraint".into(),
                                description: "The constraint on the maximum number of pods that can be run".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "network_config".into(),
                                description: "Networking configuration for this NodePool. If specified, it overrides the".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "node_drain_config".into(),
                                description: "Specifies the node drain configuration for this node pool.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "placement_policy".into(),
                                description: "Specifies the node placement policy.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "queued_provisioning".into(),
                                description: "Specifies the configuration of queued provisioning.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "upgrade_settings".into(),
                                description: "Upgrade settings control disruption and speed of the upgrade.".into(),
                                field_type: smelt_provider::FieldType::Record(vec![]),
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                            smelt_provider::FieldSchema {
                                name: "version".into(),
                                description: "The version of Kubernetes running on this NodePool's nodes. If unspecified,".into(),
                                field_type: smelt_provider::FieldType::String,
                                required: false,
                                default: None,
                                sensitive: false,
                            },
                        ],
                    },
                ],
            },
        }
    }

    pub(super) async fn create_container_nodepool(
        &self,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        // Extract fields from config
        let autoscaling = config.pointer("/config/autoscaling").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodePoolAutoscaling>(
                v.clone(),
            )
            .ok()
        });
        let best_effort_provisioning = config.pointer("/config/best_effort_provisioning").and_then(
            |v| {
                serde_json::from_value::<google_cloud_container_v1::model::BestEffortProvisioning>(
                    v.clone(),
                )
                .ok()
            },
        );
        let conditions = config.pointer("/config/conditions").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_container_v1::model::StatusCondition>>(
                v.clone(),
            )
            .ok()
        });
        let config_val = config.pointer("/config/config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeConfig>(v.clone()).ok()
        });
        let initial_node_count = config.optional_i64("/config/initial_node_count");
        let locations = config
            .pointer("/config/locations")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let management = config.pointer("/config/management").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeManagement>(v.clone())
                .ok()
        });
        let max_pods_constraint = config.pointer("/config/max_pods_constraint").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MaxPodsConstraint>(v.clone())
                .ok()
        });
        let name = config.require_str("/identity/name")?.to_string();
        let network_config = config.pointer("/config/network_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeNetworkConfig>(v.clone())
                .ok()
        });
        let node_drain_config = config.pointer("/config/node_drain_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::NodeDrainConfig>(
                v.clone(),
            )
            .ok()
        });
        let placement_policy = config.pointer("/config/placement_policy").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::PlacementPolicy>(
                v.clone(),
            )
            .ok()
        });
        let queued_provisioning = config.pointer("/config/queued_provisioning").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_container_v1::model::node_pool::QueuedProvisioning,
                >(v.clone())
                .ok()
        });
        let upgrade_settings = config.pointer("/config/upgrade_settings").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::UpgradeSettings>(
                v.clone(),
            )
            .ok()
        });
        let version = config.optional_str("/config/version").map(String::from);

        // Build SDK model
        let mut model = google_cloud_container_v1::model::NodePool::default();
        if let Some(v) = autoscaling {
            model = model.set_autoscaling(v);
        }
        if let Some(v) = best_effort_provisioning {
            model = model.set_best_effort_provisioning(v);
        }
        if let Some(v) = conditions {
            model = model.set_conditions(v);
        }
        if let Some(v) = config_val {
            model = model.set_config(v);
        }
        if let Some(v) = initial_node_count {
            model = model.set_initial_node_count(i32::try_from(v).map_err(|_| {
                ProviderError::InvalidConfig(format!(
                    "initial_node_count: value {v} out of range for i32"
                ))
            })?);
        }
        if let Some(v) = locations {
            model = model.set_locations(v);
        }
        if let Some(v) = management {
            model = model.set_management(v);
        }
        if let Some(v) = max_pods_constraint {
            model = model.set_max_pods_constraint(v);
        }
        model = model.set_name(name.clone());
        if let Some(v) = network_config {
            model = model.set_network_config(v);
        }
        if let Some(v) = node_drain_config {
            model = model.set_node_drain_config(v);
        }
        if let Some(v) = placement_policy {
            model = model.set_placement_policy(v);
        }
        if let Some(v) = queued_provisioning {
            model = model.set_queued_provisioning(v);
        }
        if let Some(v) = upgrade_settings {
            model = model.set_upgrade_settings(v);
        }
        if let Some(v) = version {
            model = model.set_version(v);
        }

        // Make API call
        let parent = config.require_str("/identity/cluster_id")?.to_string();
        self.cluster_manager()
            .await?
            .create_node_pool()
            .set_parent(&parent)
            .set_node_pool(model)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("Create_node_pool NodePool", e))?;

        let provider_id = format!("{}/nodePools/{}", parent, name);
        self.read_container_nodepool(&provider_id).await
    }

    pub(super) async fn read_container_nodepool(
        &self,
        provider_id: &str,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        let node_pool = self
            .cluster_manager()
            .await?
            .get_node_pool()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("GetNodePool", e))?;

        let state = serde_json::json!({
            "identity": {
                "name": node_pool.name.as_str(),
            },
            "config": {
                "autoscaling": &node_pool.autoscaling,
                "best_effort_provisioning": &node_pool.best_effort_provisioning,
                "conditions": &node_pool.conditions,
                "config": &node_pool.config,
                "initial_node_count": node_pool.initial_node_count,
                "locations": &node_pool.locations,
                "management": &node_pool.management,
                "max_pods_constraint": &node_pool.max_pods_constraint,
                "network_config": &node_pool.network_config,
                "node_drain_config": &node_pool.node_drain_config,
                "placement_policy": &node_pool.placement_policy,
                "queued_provisioning": &node_pool.queued_provisioning,
                "upgrade_settings": &node_pool.upgrade_settings,
                "version": node_pool.version.as_str(),
            },
        });

        let mut outputs = HashMap::new();
        outputs.insert("name".into(), serde_json::json!(&node_pool.name));

        Ok(ResourceOutput {
            provider_id: provider_id.to_string(),
            state,
            outputs,
        })
    }

    pub(super) async fn update_container_nodepool(
        &self,
        provider_id: &str,
        config: &serde_json::Value,
    ) -> Result<ResourceOutput, ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        // Extract fields from config
        let _autoscaling = config.pointer("/config/autoscaling").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodePoolAutoscaling>(
                v.clone(),
            )
            .ok()
        });
        let _best_effort_provisioning = config
            .pointer("/config/best_effort_provisioning")
            .and_then(|v| {
                serde_json::from_value::<google_cloud_container_v1::model::BestEffortProvisioning>(
                    v.clone(),
                )
                .ok()
            });
        let _conditions = config.pointer("/config/conditions").and_then(|v| {
            serde_json::from_value::<Vec<google_cloud_container_v1::model::StatusCondition>>(
                v.clone(),
            )
            .ok()
        });
        let _config_val = config.pointer("/config/config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeConfig>(v.clone()).ok()
        });
        let _initial_node_count = config.optional_i64("/config/initial_node_count");
        let locations = config
            .pointer("/config/locations")
            .and_then(|v| serde_json::from_value::<Vec<String>>(v.clone()).ok());
        let _management = config.pointer("/config/management").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeManagement>(v.clone())
                .ok()
        });
        let _max_pods_constraint = config.pointer("/config/max_pods_constraint").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::MaxPodsConstraint>(v.clone())
                .ok()
        });
        let _network_config = config.pointer("/config/network_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::NodeNetworkConfig>(v.clone())
                .ok()
        });
        let _node_drain_config = config.pointer("/config/node_drain_config").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::NodeDrainConfig>(
                v.clone(),
            )
            .ok()
        });
        let _placement_policy = config.pointer("/config/placement_policy").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::PlacementPolicy>(
                v.clone(),
            )
            .ok()
        });
        let _queued_provisioning = config.pointer("/config/queued_provisioning").and_then(|v| {
            serde_json::from_value::<
                    google_cloud_container_v1::model::node_pool::QueuedProvisioning,
                >(v.clone())
                .ok()
        });
        let _upgrade_settings = config.pointer("/config/upgrade_settings").and_then(|v| {
            serde_json::from_value::<google_cloud_container_v1::model::node_pool::UpgradeSettings>(
                v.clone(),
            )
            .ok()
        });
        let version = config.optional_str("/config/version").map(String::from);

        // UpdateNodePool uses set_name + individual field setters (not model body)
        let mut req = self
            .cluster_manager()
            .await?
            .update_node_pool()
            .set_name(provider_id);
        if let Some(v) = locations {
            req = req.set_locations(v);
        }
        if let Some(v) = version {
            req = req.set_node_version(v);
        }
        req.send()
            .await
            .map_err(|e| super::classify_gcp_error("update_node_pool NodePool", e))?;

        self.read_container_nodepool(provider_id).await
    }

    pub(super) async fn delete_container_nodepool(
        &self,
        provider_id: &str,
    ) -> Result<(), ProviderError> {
        let _name = provider_id
            .rsplit('/')
            .next()
            .unwrap_or(provider_id)
            .to_string();
        self.cluster_manager()
            .await?
            .delete_node_pool()
            .set_name(provider_id)
            .send()
            .await
            .map_err(|e| super::classify_gcp_error("DeleteNodePool", e))?;
        Ok(())
    }
}

// Diff: fields that force replacement
pub(super) fn container_cluster_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}

// Diff: fields that force replacement
pub(super) fn container_nodepool_forces_replacement(path: &str) -> bool {
    matches!(path, "identity.name")
}
